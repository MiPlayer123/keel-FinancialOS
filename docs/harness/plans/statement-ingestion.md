# Statement Ingestion Plan — Upload → Extract (staged) → Suggest → Approve → Reconcile

> Produced 2026-07-19 by a planning agent (full codebase read). Card-payment linking + investment holdings verification included. Status: awaiting adversarial Codex review before build.

## 0. What exists today (verified — do not rebuild)

**Receipts flow (the template to mirror):** upload via `/documents/upload-url` (signed Storage PUT, bucket `receipts`/`statements`, path `{householdId}/{documentId}/{uuid}`) → `/documents/confirm-upload` (server downloads, verifies size/mime, sha256, `keel_documents_confirm_upload`; migration `20260717234500_documents_attach_only.sql`: `documents`, immutable `document_versions` unique `(document_id, content_sha256)`, `document_attachments` with **`statement_id` target already wired**) → for receipts enqueues `receipt_extract` on pgmq `sync_events` → `worker/receipt-extract.ts` persists immutable `document_extractions` (migration `20260718170000`, idempotent per `(document_version_id, extractor, extractor_version)`) → deterministic candidate SQL + pure `packages/documents/src/matcher.ts` → `document_transaction_matches` status `suggested` → `receipts.decide_match`/`detach_match` → UI `apps/web/src/app/dashboard/receipts/page.tsx`.

**Statements infra already live (shapes the plan):** `statements`, `statement_lines`, `reconciliation_sessions/items/adjustments`, `close_checklists` (migration `20260712150000_reconciliation.sql`; BC-v2.1 lines 166–179, gate 8). `statements` has `unique(household_id, account_id, period_start, period_end, source_hash)` and `keel_statement_create` **enforces `opening + Σ lines = ending`**. Commands `statements.create/set_cadence`, `reconciliations.close/reopen`; queries `statements.list/cadence`. Full UI `apps/web/src/app/dashboard/statements/page.tsx` (manual line entry, reconcile-and-close, reopen-with-reason, cadence/overdue F-029, attach-only PDF upload via `AttachmentsSection targetType="statement"`). `statements` Storage bucket exists. `CreateStatementPayloadSchema` in contracts (money as minor strings, lines min1 max5000, sourceHash 64-hex). Transfers: `transfer_links`, `keel_detect_transfers`, manual `keel_link_transfer`. Holdings: `holdings` (`source in ('manual','plaid')`, unique `(account_id, as_of, symbol, source)`), `holdings_snapshots`. Red-team: `RED_TEAM_STRINGS`, `tests/integration/06-redteam.test.ts`, receipt injection fixtures, prompt-fence tests.

**Note:** `packages/imports` (named in CLAUDE.md) doesn't exist; actual package is `packages/ingest`. Statement lines are *independent evidence* (BC-v2.1 line 179), never canonical transactions → home is `packages/documents` + reconciliation tables, NOT ingest.

**The actual gap** (per F-029 deferral: "CSV line import DEFERRED — uploads stay attach-only, lines hand-keyed"): parsing an uploaded file into a **draft** the user approves into `statements.create`; card-payment↔statement linking; investment holdings from statements.

## 1. Design decisions
1. **Reuse, don't fork.** Upload = existing document routes with `kind='statement'`. Staging mirrors receipts: immutable typed extraction row + mutable suggest→approve decision row. Approval materializes into existing `statements`/`statement_lines` via existing validation — extraction never touches canonical tables directly (Laws 5, 2/10 class B).
2. **Statements need their own staging tables.** `document_extractions` is receipt-shaped. New `statement_extractions` (+ typed child line/holding tables, BIGINT columns not money-in-jsonb — Law 4) and `statement_drafts` (decision row). No parallel upload pipeline invented.
3. **Deterministic parsers first-class.** CSV + OFX/QFX parse with pure hand-rolled TS in `packages/documents` (no new deps; vendor bundle already ships `@keel/documents` to worker). PDF via AI extractor in `packages/ai` (fixture default; cloud wiring a human ⚑). Both land in same staging tables with `extractor` provenance + confidence (deterministic=1.0, AI=clamped model-reported). Both still suggest→approve.
4. **Idempotency (Law 9):** same bytes → same sha256 → version unique no-ops; one extraction per `(version, extractor, extractor_version)`; one draft per version; approved statement uses file sha256 as `statements.source_hash` → existing unique key blocks dupes across re-uploads; confirm-upload returns `duplicate:true` when a statement-kind version with same hash exists.
5. **Account binding explicit at upload** (required in upload dialog, stored on draft). Extractor's institution/mask read kept as inert *hint* used only to warn on mismatch (explicit ownership, Law 9 §9.1).
6. **Card-payment linking** = deterministic matcher (Law 1) → class-B suggestions in `statement_payment_links`: for a liability-account statement, find card-side inflow within `[period_end, period_end+35d]` equal to `|ending_minor|` (exact first; in-window candidates lower-scored; prefer existing `transfer_links` pairs). Confirm/reject/detach mirrors `document_transaction_matches`.
7. **Investment statements** → positions in `statement_extraction_holdings`; approval for investment-subtype account offers separate class-B `statements.apply_holdings` writing `holdings` with new `source='statement'` at `as_of=period_end` (idempotent). Discrepancies vs current = read-model diff, never auto-corrected.
8. **Contract amendments (log in NOTES.md):** investment/PDF statements can't satisfy strict `opening+Σ=ending` → add `statements.balance_check` (`'strict'` default | `'anchor'`); relax `CreateStatementPayloadSchema.lines` min1→min0 and `CloseReconciliationPayloadSchema.items` min1→min0 only when zero lines. Default strict behavior unchanged.

## 2. Storage
Unchanged bucket `statements`, path minted by `/documents/upload-url`. Original immutable (`document_versions` + `keel_forbid_mutation`). Change: `DOCUMENT_MIME_ALLOWLIST` (api/index.ts line 57) becomes per-kind — receipt unchanged; statement = pdf, csv, ms-excel, x-ofx, octet-stream only when filename ends `.csv|.ofx|.qfx` (browsers report empty type for OFX; re-validate at confirm-upload; worker parser is final arbiter — unparseable → `failed`, never a guess).

## 3. Migrations (one per slice, applied per ops-facts psql ritual)
- **`20260720000000_statement_extraction.sql`**: `statement_extract_kind` enum; `statement_extractions` (period, opening/ending minor, currency, kind_hint, account_hint ≤200 chars, confidence, raw_evidence jsonb inert, unique `(version, extractor, extractor_version)`, append-only trigger); `statement_extraction_lines` (typed BIGINT, unique `(extraction_id, line_no)`, immutable); `statement_extraction_holdings` (symbol/qty/price_minor/value_minor/cost_basis_minor, unique `(extraction_id, symbol)`, immutable); `statement_draft_status` enum; `statement_drafts` (version unique FK, account_id nullable, status, extraction_id, statement_id FK, decided_by/at — mutable status row, dismiss = flip not delete). Grants/RLS per receipts pattern. Procs: `keel_worker_persist_statement_extraction` (definer owner keel_worker, service_role only, atomic insert + child rows, money strings ::bigint, on-conflict no-op replay, upsert draft to suggested/failed); `keel_statement_drafts` (viewer read + discrepancy preview = ledger balance at period_end − extracted ending); `keel_cmd_statements_approve_draft` (class-B; payload = full statement body user saw/edited, Law 11; validates like `keel_statement_create` strict unless anchor; inserts statements+lines with source_hash = version sha256; flips draft; inserts document_attachments linking source file; idempotent); `keel_cmd_statements_dismiss_draft`. Export: extend `keel_export_household` (rename-and-wrap) to include the 4 new tables.
- **`20260720010000_statement_anchor_mode.sql`**: add `statements.balance_check` (strict|anchor); amend `keel_statement_create` sum check to skip when anchor; amend `keel_reconciliation_close` to accept zero items for zero-line statements. NOTES.md amendment citing BC-v2.1 179 + 402.
- **`20260720020000_statement_payment_links.sql`**: `statement_payment_status` enum; `statement_payment_links` (statement_id, canonical_transaction_id = card-side payment, transfer_link_id nullable, status, score, reason_codes, unique `(statement_id, canonical_transaction_id)`; NO partial-unique-confirmed — partial payments allowed); `keel_statement_suggest_payments` (deterministic SQL, definer, authenticated+service_role; window `[period_end, +35d]`, exact-amount=100 else lower, prefer transfer_links pairs, on-conflict no-op); `keel_cmd_statements_decide_payment_link`/`detach_payment_link` (partner, audited; confirm may upsert transfer_links suggestion via keel_link_transfer — still user-confirmed on Review). Extend `keel_list_statements` with `paymentLinks`.
- **`20260720030000_statement_holdings_apply.sql`**: widen `holdings.source` to include `'statement'`; `keel_cmd_statements_apply_holdings` (validates investment-subtype + positions match extraction rows, upserts holdings at as_of=period_end source=statement, appends snapshots, audited); read model `keel_statement_holdings_diff` (per-symbol qty/value delta, suggestions only).

## 4. Pure packages
- **`packages/documents/src/statement/`** (auto-vendored via `@keel/documents` alias): `types.ts` (defensive-parse, every field nullable, hostile input → inert nulls never throw-into-guess); `csv.ts` (RFC-4180 + header alias mapper, debit/credit split, `$`/`,`/paren negatives, decimal→minor by **string math** no floats; CSVs → lines + inferred period, balances null → user keys them); `ofx.ts` (tolerant OFX 1.x SGML / 2.x XML; bank/card STMTRS/CCSTMTRS → lines + LEDGERBAL→ending; investment INVSTMTRS → INVPOSLIST + SECLIST → holdings); `payment-matcher.ts` (pure mirror for property tests).
- **`packages/ai/src/statement.ts`**: `StatementExtractor` interface, `RecordedStatementExtractor` (fixture keyed by sha256, CI path), `CloudStatementExtractor` (fetch-based, key from config never logged — Law 12, live = human ⚑); `STATEMENT_PROMPT_VERSION`; prompt fences document as data with explicit embedded-instruction refusal (mirror `buildReceiptExtractionPrompt`), instructs minor-unit integer strings.

## 5. Worker
- **`supabase/functions/worker/statement-extract.ts`** (mirrors receipt-extract): resolve version→document→household (never trust message refs); route csv/ofx/pdf; persist via `keel_worker_persist_statement_extraction`; failed downloads/parses persist `status='failed'` + short error_code, never provider bodies. **No RPCs beyond the persist allowlist** (Law 5).
- **`worker/index.ts`**: add `statement_extract` branch beside `receipt_extract`.
- **`api/index.ts` /documents/confirm-upload**: for `kind='statement'` accept optional `accountId` (validated+authz'd), create `statement_drafts` row status pending, enqueue `statement_extract` + kick drain (best-effort; queue failure never undoes durable upload); return `duplicate:true` on hash match.
- Deploy: `node scripts/build-functions.mjs && supabase functions deploy api worker`.

## 6. Contracts / authz / API map
- Contracts: `ApproveStatementDraftPayloadSchema` (CreateStatement shape + draftId + balanceCheck, lines min0), `DismissStatementDraftPayloadSchema`, `DecideStatementPaymentLinkPayloadSchema`, `DetachStatementPaymentLinkPayloadSchema`, `ApplyStatementHoldingsPayloadSchema`; amend CreateStatement lines min0 + optional balanceCheck, CloseReconciliation items min0.
- authz: commands `statements.approve_draft/dismiss_draft/decide_payment_link/detach_payment_link/apply_holdings` at `partner`; queries `statements.drafts/holdings_diff` at `viewer`. (Law 7; BC-v2.1 statements.* family line 324.)
- api: COMMAND_TO_PROC += 5; QUERY_TO_PROC += holdings_diff; bespoke route `/statements/drafts` (mirrors `/receipts/inbox`, signs per-row 5-min read URLs).
- `keel-api.ts`: `uploadStatement` (passes accountId at confirm), `fetchStatementDrafts`, `approveStatementDraft`, `dismissStatementDraft`, `decideStatementPaymentLink`, `detachStatementPaymentLink`, `applyStatementHoldings`; extend `StatementRow` with paymentLinks, new `StatementDraftRow`.

## 7. UI slices (desktop-first, 390px — Law 8)
1. `statements/page.tsx`: "Upload statement" dialog (required account, file `.pdf,.csv,.ofx,.qfx`); new **Drafts** section (filename, account, extractor badge CSV/OFX=deterministic vs AI+confidence%, extracted period/opening/ending, line count, ledger-delta preview, account-hint mismatch warning; Review&approve / Dismiss). Review opens existing `CreateStatementDialog` prefilled, submits `statements.approve_draft` (user sees exactly what's written, Law 11).
2. Statement detail: "Card payment" block for liability accounts (suggested payment links, Confirm/Reject/detach; "Find payment" on demand).
3. Investment drafts: holdings table + diff vs current + "Apply positions" action.
4. `accounts/[id]` upload shortcut pre-bound to account (optional).

## 8. Reconciliation hook
No change to close semantics (gate 8). Approved statements arrive with lines extracted → existing auto-match in CloseStatementDialog does the work; payment link confirms card-payment transfer; anchor statements reconcile on balances+adjustments. Cadence F-029 picks up uploaded statements automatically.

## 9. Tests
Pure csv/ofx tests (header-alias matrix, split columns, paren negatives, string-math money identity, OFX 1.x/2.x bank/card/investment fixtures, malformed→failed never throw). **Red-team (CI-blocking, Law 5):** CSV/OFX with `RED_TEAM_STRINGS` in description/MEMO → stored verbatim inert; worker test proves only persist-allowlist RPCs invoked; e2e via 06-redteam pattern → hostile text survives byte-identical into statement_lines after human approval, only approved rows written. `packages/ai/test/statement.test.ts` (prompt fences + refuses embedded instructions + float money → null). `worker/test/statement-extract.test.ts` (idempotent redelivery, household mismatch refusal, download-fail→failed, mime routing). `tests/integration/26-statement-drafts.test.ts` (upload→extract→approve; re-upload no-op; approve twice idempotent; strict-sum-mismatch rejected; dismissed stays dismissed; payment-link suggest→confirm→detach; tenant isolation; export includes new tables). Payment-link matcher precision fixture before that slice ships. `pnpm build` before every web push.

## 10. Staged delivery (one PR each, branch off main)
| # | Slice | Files |
|---|---|---|
| 1 | Pure parsers + types + red-team fixtures (tests first) | `packages/documents/src/statement/*`, tests |
| 2 | AI statement extractor (fixture + cloud stub + prompt tests) | `packages/ai/src/statement.ts`, test |
| 3 | Staging migration + anchor-mode + export extension | `20260720000000`, `…010000`; NOTES amendment |
| 4 | Contracts + authz + api map + per-kind mime + confirm-upload enqueue + `/statements/drafts` | contracts, authz, api/index.ts |
| 5 | Worker job + dispatch + tests + deploy | `worker/statement-extract.ts`, `worker/index.ts`, test |
| 6 | Web: upload dialog + drafts inbox + prefilled approve | `keel-api.ts`, statements page |
| 7 | Payment links: migration + matcher + commands + UI | `…020000`, api map, statements page |
| 8 | Investment holdings: migration + apply + diff UI | `…030000`, investments/statements UI |
| 9 | Integration tests + accounts shortcut + polish | `tests/integration/26-statement-drafts.test.ts` |

Human ⚑: enabling cloud PDF extractor (`AI_PROVIDER=cloud`) — same gate as receipts.
