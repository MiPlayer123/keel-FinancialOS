# Census — quicken-simplifi-iphone-01

## Evidence
- `iphone-01-APPLE_T_05_LIGHT_2064x2752.png` (App Store marketing template, phone mockup inset on blue-violet background) → composite showing a "Retirement" projection panel (area chart) stacked above an "Investments" account panel (balance + sparkline), under headline "Turn 'what ifs' into an actual plan."
- `iphone-02-APPLE_T_02_LIGHT_2064x2752.png` (marketing template) → "Smart budgeting" panel: a "Left this month" total, a donut chart, and an itemized category list (Income, Bills, Planned Spend, Other Spend, Savings Goals), under headline "Smart budgeting."
- `iphone-03-APPLE_T_03_LIGHT_2064x2752.png` (marketing template) → "Net Income" bar+line chart (income vs. expenses vs. net income, Apr–July) followed by three business income line items and the top edge of the same "Left this month" donut seen in iphone-02, under headline "Business & personal finances in one clear view."
- `iphone-04-0x0ss-6.png` (raw device screenshot, 1290×2796, no marketing chrome) → business income line items (Consultation/Cabinet Design/Installation) followed by an "Invoices" section: a "Paid in full" confirmation card, then invoice `#1027` detail (From/To/Sent fields).
- `iphone-05-APPLE_T_01_LIGHT_2064x2752.png` (marketing template) → a "Quicken" wordmark card containing a "Projected Cash Flow" line chart (solid-to-dashed) with a callout, then a "Banking" account summary (Cash & Checking, Credit, Savings) and two business income line items, under headline "Know what's next with your money."
- `iphone-06-PromotionalImage-1024x1024-4.png` (1024×1024 App Store icon/promo tile, not a UI screen) → blue-violet gradient square with white "Quicken" wordmark/speech-bubble mark and subtitle "Business & Personal."
- `iphone-07-APPLE_T_06_LIGHT_2064x2752.png` (marketing template) → identical content to iphone-04 (business income items, "Paid in full" card, invoice `#1027` detail) framed with headline "Run your business confidently."
- `iphone-08-0x0ss-3.png` (raw device screenshot, 1290×2796) → "Net Income" bar+line chart (Apr–Jun, 3 months) plus business income line items and the top edge of a "Left this month" donut, under implied headline theme "Business & personal finances in one clear view" (same content family as iphone-03, three months instead of four, no marketing headline text baked into this particular capture's visible crop).

Note on evidence quality: six of the eight files (`iphone-01`, `02`, `03`, `05`, `07`, and the icon `06`) are App Store marketing template renders — a blue-violet background with headline/subhead copy and a white rounded card containing what reads as a composite of multiple in-app panels stacked into one continuous "hero" image, not a literal single screen a user scrolls. Only `iphone-04` and `iphone-08` are undecorated device screenshots (full-bleed white background, no headline copy, no status bar or nav chrome visible in the crop), and even these show content that looks assembled from more than one logical screen (a payment-confirmation card immediately followed by a full invoice detail record). Findings below describe what is drawn, while flagging this composite/marketing nature wherever it affects interpretation.

## Information architecture
- No tab bar, sidebar, hamburger menu, status bar, or back/nav chrome is visible in any of the 8 images — every image is cropped to content panels only. This record cannot establish the app's top-level navigation shell, or what other destinations exist alongside the panels shown.
- Distinct content panels recur across files and appear to be assembled versions of underlying app screens/widgets:
  1. **Retirement** panel (area/fan chart by age) — iphone-01 only.
  2. **Investments** panel (balance, % change badge, sparkline) — iphone-01 only.
  3. **Left this month** / budget panel (total, donut, itemized category rows: Income, Bills, Planned Spend, Other Spend, Savings Goals) — iphone-02 fully, top edge only visible in iphone-03 and iphone-08.
  4. **Net Income** panel (income/expense bars + net-income dashed line, monthly) — iphone-03 (Apr–July, 4 months) and iphone-08 (Apr–Jun, 3 months); same chart, different month ranges, implying this is a live/rendered chart rather than a fixed marketing asset.
  5. **Projected Cash Flow** panel (solid-to-dashed line chart with "Today" marker and callout) — iphone-05 only, appears directly under a centered "Quicken" wordmark inside the card, suggesting this may be a dashboard/home-screen header treatment.
  6. **Banking** account-summary panel (Cash & Checking / Credit / Savings, no chart) — iphone-05 only, positioned directly beneath the Projected Cash Flow panel.
  7. **Business income line items** (Consultation / Cabinet Design / Installation, as pill rows) — recurs in iphone-03 (with cent precision under "Net Income"), iphone-04, iphone-05, iphone-07, iphone-08 (iphone-03/08 show larger "+$8,350.00" for Consultation vs. "+$150.00" in iphone-04/05/07 — two different data snapshots of the same three line-item labels).
  8. **Invoices** panel — "Paid in full" confirmation sub-card plus invoice `#1027` detail record (From/To/Sent) — iphone-04 and iphone-07 only.
- The business income line items and the "Invoices" panel are shown directly adjacent/stacked in iphone-04 and iphone-07, implying (within a single business context) income tracking and invoicing live close together in the IA, but no chrome confirms whether they are one screen, two screens joined by scroll, or a composite for marketing purposes only.
- The "Banking" panel (iphone-05) groups three account-type rows — Cash & Checking (headline figure), Credit, Savings — under one "Banking" label, implying accounts are grouped by type into a single rollup rather than shown as a flat list.
- "Quicken" as a centered wordmark appears inside the Projected Cash Flow card in iphone-05 only; no other panel carries an in-card brand mark, so its structural meaning (e.g., a dashboard/home screen labeled with the product name) is not confirmed by the other 7 images.

## Layout & content
- **Retirement panel (iphone-01):** Title "Retirement" (left-aligned, dark serif-free heading) over a stacked/layered area chart (purple top layer, green middle layer, yellow-green bottom layer — likely three cumulative components of projected retirement assets). Y-axis gridline labels "$3m," "$2m," "$1m" (top to bottom); x-axis labels "55," "65," "75" (ages). A green vertical marker line rises from the x-axis to a callout "$2.4mil" (bold, black, large) positioned just before age 65, with the marker line touching the chart's purple/green boundary at that point — the money figure sits directly above its own reference line on the chart, with implied context "at approximately age 63–64."
- **Investments panel (iphone-01):** Title "Investments" (left-aligned). Large bold figure "$200,725" directly beneath. Immediately below that: a green-outlined rounded-pill badge "↑ 0.92%" (up arrow + percent, green text/icon on white fill with green border) followed by plain green text "+$39.92 today" — three pieces of same-color (green) context sitting directly beside the headline balance: relative % change, absolute today's change, and an implicit "gain" framing via color. Below, a sparkline/area chart with y-axis gridlines "$210k," "$200k," "$190k," "$180k" and a blue/purple filled line trending upward left to right with visible daily jaggedness (no x-axis date labels visible in the crop).
- **Budget/"Left this month" panel (iphone-02, fullest capture):** Centered large bold figure "$1,415" with centered subtitle "Left this month" directly beneath it — status label sits immediately under the number, not beside it. Below that, a five-segment donut chart (light blue largest wedge, indigo/purple second, green third, yellow-green fourth, magenta/pink smallest) with a plain white hole (no center total text, unlike some competitor donuts). Below the donut, a list of category rows, each: a small rounded-square color-chip icon (colored fill, white glyph) + label (left) + signed dollar amount (right, colored) + trailing chevron ">" (far right):
  - Green chip, white "+" glyph — "Income" — **+$4,000** (green text)
  - Dark indigo/purple chip, white "−" glyph — "Bills" — **-$1,385** (black/dark text, not red, despite negative sign)
  - Light-blue chip, white "−" glyph — "Planned Spend" — **-$600** (black/dark text)
  - Magenta/pink chip, white "−" glyph — "Other Spend" — **-$400** (black/dark text)
  - Yellow-green chip (glyph not legible, row cut off at frame bottom) — "Savings Goals" — **-$200** (partially visible, cut off)
  - Each chip's fill color matches one donut-wedge color 1:1 (green chip↔green wedge, indigo chip↔indigo wedge, etc.), so the donut functions as a color-legend for the list beneath it, with no separate text legend.
- **Net Income panel (iphone-03, iphone-08):** Title "Net Income" (left-aligned). Grouped bar chart: green bars = Income, blue/purple bars = Expenses, one bar-pair per month (iphone-03: Apr/May/Jun/July — 4 months; iphone-08: Apr/May/Jun — 3 months). Y-axis gridlines "$8K," "$6K," "$4K," "$2K." A black dashed line with solid black dot markers threads across the top of each month's bar pair, trending upward left→right, representing "Net income" per a labeled legend row beneath the chart: green swatch "Income," purple swatch "Expenses," black swatch "Net income." Below the chart, business income line items rendered as light-gray, fully rounded (pill-shaped) rows, each: label (left, black) + signed amount (right, green, "+$" prefix, two decimals) — "Consultation +$8,350.00," "Cabinet design +$375.00," "Installation +$900.00" (iphone-03/08 snapshot — larger Consultation figure than the iphone-04/05/07 snapshot). Beneath the last pill, the top edge of a "Left this month" card is visible (title only, donut cut off by frame).
- **Projected Cash Flow + Banking panel (iphone-05):** Centered "Quicken" wordmark (purple, brand logotype) at the top of the white card. Below it, left-aligned section title "Projected Cash Flow." Line chart: solid blue line from the left edge to a vertical black guide line labeled "Today" (directly above the axis, marking present-day), after which the line becomes dashed — visually distinguishing historical/actual (solid) from projected/future (dashed) on the same line without a second legend. Y-axis gridlines "$35k," "$30k," "$25k," "$20k." A callout bubble "$31,452" with a solid dot marker pins a specific future point on the dashed line (a local peak partway along the timeline); the line continues rising to at least one more peak beyond the callout, exceeding the $35k gridline at the right edge (cut off by frame). Below the chart: title "Banking," large bold figure "$87,762" with subtitle "Cash & Checking" directly beneath (status/label immediately under the number, same pattern as the Investments panel). Two further rows, plain text (no chip icon, no pill background), label left / amount right: "Credit **-$10,553**" and "Savings **$80,107**" — both rendered in plain black, not red, despite Credit being a negative/debt figure. Below a divider, two business income pill rows: "Consultation +$150.00," "Cabinet Design +$375.00" (note capitalization differs from iphone-03/08's "Cabinet design" — inconsistent capitalization of the same label across captures).
- **Invoices panel (iphone-04, iphone-07 — identical content, iphone-07 in marketing frame):** Three business income pill rows atop the panel: "Consultation +$150.00," "Cabinet Design +$375.00," "Installation +$900.00" (all green, two-decimal). Section header "Invoices" (bold, left-aligned). A light-gray card beneath contains: a green circular checkmark icon (left) beside bold text "Paid in full"; below that, plain gray/black body text "Your direct deposit payment of $1,110.00 was approved on Jun 1"; below that, "Confirmation #: 478567." Beneath this card (no visible container boundary distinguishing it from the "Paid in full" card, they read as continuous), an invoice detail record begins: bold header "#1027," a full-width thin divider rule, then key-value rows each separated by a divider rule, label bold-left / value right-aligned, values right-aligned as multi-line where needed:
  - "From" → "Verrow Designs" / "mo@verrowdesigns.com" / "(408) 272-0098" (three lines, right-aligned, business name bold-weight, contact details lighter)
  - "To" → "Jack Pine" / "The Pineapple Company" (two lines, right-aligned)
  - "Sent" → "May 1" (single line, right-aligned)
- Money-figure formatting throughout this unit: headline/summary figures (e.g. "$200,725," "$87,762," "$1,415," "$2.4mil") are shown rounded to whole dollars (or "mil" abbreviation); itemized/line-item figures (e.g. "+$150.00," "+$8,350.00," "-$10,553" is an exception without decimals) mostly carry two decimal places with comma thousands separators; the "$31,452" chart callout and "-$10,553"/"$80,107" account rows are whole-dollar with commas, no decimals — so decimal precision is not fully consistent even within this one unit (compare "+$150.00" pill rows against "-$10,553" plain rows).

## Controls inventory
- Trailing chevron ">" on every budget category row (Income, Bills, Planned Spend, Other Spend, Savings Goals — iphone-02) — implies each row is tappable, presumably drilling into that category's transactions; no chevron appears on the business income pill rows (Consultation, Cabinet Design, Installation) in any capture, so those may not share the same drill-down affordance.
- Colored rounded-square icon chips (green "+", indigo "−", light-blue "−", magenta "−", yellow-green "−" cut off) preceding each budget category row (iphone-02) — function as a category-type badge (income vs. three/four expense sub-types) and simultaneously as the donut-chart legend key (matching wedge colors).
- Green-outlined pill badge "↑ 0.92%" (iphone-01, Investments panel) — a status badge combining a direction glyph and a percentage, distinct from the plain-text "+$39.92 today" beside it.
- Chart legend swatches (colored squares + text) beneath the Net Income bar chart: green "Income," purple "Expenses," black "Net income" (iphone-03, iphone-08) — static legend, not shown as interactive/toggleable in the evidence.
- Solid-dot chart markers with attached text callouts ("$2.4mil" on the Retirement chart, "$31,452" on the Projected Cash Flow chart) — a direct-labeling technique pinning a specific figure to a specific point on the line/area, in lieu of a hover/tooltip (none of these are interactive screenshots, so tap/hover behavior is not confirmed).
- Vertical guide line labeled "Today" on the Projected Cash Flow chart (iphone-05) — a fixed reference marking the present moment on the timeline, distinguishing already-realized (solid line, left of "Today") from projected (dashed line, right of "Today").
- Green circular checkmark icon beside "Paid in full" (iphone-04, iphone-07) — a status badge for a completed/settled invoice payment.
- Business income rows are rendered as fully rounded, light-gray-filled "pill" rows throughout every panel that shows them — a consistent, reused list-row style distinct from the plain-text rows used for account balances ("Credit," "Savings") and the chip+chevron rows used for budget categories.

## Flow steps
N/A — no image in this unit shows a multi-step interactive sequence (e.g., creating an invoice, categorizing a transaction, running a retirement scenario). The apparent adjacency of the "Paid in full" confirmation card and the invoice `#1027` detail record in iphone-04/iphone-07 looks sequence-like (payment confirmed → view invoice) but no numbered steps, transition, or second discrete screen state is evidenced — it reads as one static composite image.

## States
- Only "populated/success" states are evidenced across all 8 images — every chart, list, and figure shown has real-looking data. No empty state, loading state, or error state (e.g., "no transactions yet," "sync failed," "add your first account") appears anywhere in this unit.
- The closest thing to a status/state indicator is the "Paid in full" confirmation card (iphone-04, iphone-07), with exact copy: "Paid in full" (headline) / "Your direct deposit payment of $1,110.00 was approved on Jun 1" / "Confirmation #: 478567" — a settled/success state for a specific payment, paired with a green checkmark icon and a traceable confirmation number.
- The green-outlined "↑ 0.92%" badge and green "+$39.92 today" (iphone-01) function as an implicit "gain/positive" state indicator for the Investments balance; no corresponding "loss/negative" state variant is shown anywhere in this unit to compare styling against.

## Business rules implied
- Negative dollar amounts are not uniformly styled red: the budget category rows "Bills -$1,385," "Planned Spend -$600," "Other Spend -$400" (iphone-02) and the account rows "Credit -$10,553" (iphone-05) all render in plain black/dark text with a leading minus sign, while positive amounts ("Income +$4,000," all business income pill rows) are rendered in green — implying this app's rule is "green marks positive/incoming money" rather than "red marks negative money," with negative figures defaulting to neutral ink plus a minus-sign glyph.
- The "Left this month" headline figure ($1,415) is computed as income minus the sum of the listed deduction categories (Bills, Planned Spend, Other Spend, Savings Goals) — evidenced by the panel's own subhead copy "See exactly what's left after bills, goals, and planned spending" (iphone-02) directly describing the calculation.
- The Projected Cash Flow chart draws a hard visual line (solid vs. dashed) at "Today," implying the underlying data model distinguishes already-occurred cash flow from projected/scheduled future cash flow within a single continuous series (iphone-05).
- The Net Income chart is shown with two different month ranges (3 months in iphone-08, 4 months in iphone-03) using the same chart chrome and the same three business income line-item labels but different dollar values ("+$8,350.00" Consultation in iphone-03/08 vs. "+$150.00" Consultation in iphone-04/05/07) — implying these are either two different underlying accounts/scenarios or two different points in time for the same demo data, not a single fixed dataset reused verbatim across marketing assets.
- An invoice ("#1027") carries its own identity distinct from the income line items that reference the same work (Consultation/Cabinet Design/Installation appear both as standalone income rows and, presumably, as the line items totaling the invoice), and the invoice detail records From/To/Sent as discrete, individually labeled fields (iphone-04, iphone-07).
- A settled invoice/payment state ("Paid in full") is paired with both a human-readable approval sentence ("...was approved on Jun 1") and a machine-style confirmation number ("478567"), implying the product exposes both a narrative and a reference-number form of the same settlement event (iphone-04, iphone-07).

## Standout details
- Distinguishing actual vs. projected data on the same line chart purely via stroke style (solid → dashed) at a labeled "Today" marker, with no separate legend entry needed for the actual/projected distinction (iphone-05) — a clean, low-chrome way to encode a temporal boundary.
- Chart callouts ("$2.4mil," "$31,452") are anchored directly to a specific point on the chart via a connecting line/dot rather than requiring a hover or tap, so the single most important number on each chart is legible in a static screenshot (iphone-01, iphone-05) — notable given these are static marketing images, but the technique would read the same live.
- The donut chart's wedge colors are reused as the leading icon-chip color on every list row beneath it (iphone-02), letting the chart double as its own legend with zero dedicated legend text.
- Pairing a plain-language settlement sentence with a discrete confirmation number in the same card — "Your direct deposit payment of $1,110.00 was approved on Jun 1" / "Confirmation #: 478567" (iphone-04, iphone-07) — gives the user both an at-a-glance narrative and an auditable reference token for the same event.
- The status/context for every headline money figure in this unit consistently sits directly *beneath* the number (e.g., "$200,725" / "↑0.92% +$39.92 today"; "$87,762" / "Cash & Checking"; "$1,415" / "Left this month") rather than beside it — a single, repeated vertical pairing convention across every panel in the unit.

## Open questions
- No panel in this unit shows navigation chrome (tab bar, status bar, header/back button), so it is not possible to confirm how a user reaches any of these screens, what sits at the same navigational level, or whether "Retirement," "Investments," "Net Income," "Projected Cash Flow," "Banking," and "Invoices" are separate top-level destinations, sub-tabs of one screen, or dashboard widgets on a single scrolling home screen.
- Whether the images depicting business income line items alongside "Invoices" (iphone-04, iphone-07) represent one continuous scrollable screen or a composited pairing of two distinct screens assembled for marketing purposes is not resolved — no scroll indicator, divider header, or screen-transition affordance is visible.
- The exact function of the trailing chevron ">" on budget category rows (iphone-02) — whether it opens a transaction list, an edit-category sheet, or something else — is not shown.
- Whether business income pill rows (Consultation/Cabinet Design/Installation) are tappable is not settled; they lack the chevron seen on budget rows but share the same rounded-pill visual treatment, which elsewhere (budget rows) does correlate with tappability.
- The precise numeric mapping in the Retirement fan chart (which of the three stacked layers — purple/green/yellow-green — represents which underlying component, e.g. contributions vs. growth vs. principal) is not labeled anywhere in the image; only the total callout "$2.4mil" is legible with confidence.
- Why "Credit -$10,553" (iphone-05) is styled in plain black rather than red or green is not explained by any visible legend; it is recorded as observed but the underlying color rule (e.g., "black for account balances regardless of sign, color reserved for transaction/flow amounts only") is inferred, not directly stated in the UI.
- The unlabeled color/legend meaning of the sparkline fill under "Investments" (iphone-01) — whether blue/purple fill indicates gain vs. loss, or is simply a fixed brand-color area chart — is not confirmed since no loss/negative variant appears in this unit for comparison.
- iphone-06 contributes no screen-level UI evidence (it is App Store icon/promo artwork only) and is excluded from all sections above except this note.
