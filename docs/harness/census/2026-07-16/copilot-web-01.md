# Census — copilot-web-01

## Evidence
- `design/references/copilot/web-01.png` → Not a raw product screenshot. It is a marketing / App-Store-style hero graphic ("Copilot Money — Your money, beautifully organized. Available for iPhone, iPad, Mac, and Web") that composites three angled, perspective-skewed device mockups over a dark-blue gradient background: one iPhone (bottom-left of the device cluster), and two macOS desktop app windows (upper-right, largest/most-cropped; lower area, second window). No standalone, flat, non-angled web screenshot is present in this unit despite the "web" group label. Source resolution is low relative to the interior UI (the device mockups occupy roughly a third of a 1200×630px image and are further shrunk/angled), so most interior text is at or below the legibility floor even after 4–8× upscaling and sharpening — this record flags every such case explicitly rather than guessing.

## Information architecture
- Both desktop windows show macOS-native window chrome: three traffic-light dots (red/yellow/green) top-left, plus a small toolbar cluster (back/forward chevrons and 1–2 additional icon buttons) — i.e., this is presented as a native Mac app window, not a browser tab/URL bar. This conflicts with the unit's "web" group label; flagged in Open questions.
- Each desktop window shows a persistent left sidebar containing:
  - A search field at the very top (magnifying-glass icon + placeholder text, placeholder itself illegible).
  - A primary nav list of ~6 short items stacked vertically, each with a small leading glyph/icon. The first item is visually selected (light-blue rounded-rect highlight/pill). Letter shapes are consistent with single-word nav labels of increasing length, but pixel resolution is too low to certify exact spelling for all six — see Open questions.
  - Below the nav list, after a gap, a secondary list of grouped rows: a short header-style row, then 1–2 indented rows each with a small colored dot bullet and a right-aligned figure, then further short header-style rows below with dot bullets and no visible adjacent figures (possibly collapsed/empty groups, or figures too small to render). This reads as an "accounts grouped by account-type" pattern (header rows = type groupings, indented rows = individual accounts with balances), but exact group/account labels are not legible with confidence.
- Main content area (both desktop windows) is a dashboard-style page: a top-left page-title label (illegible, short word), a large chart card ("Monthly ___" style label + big spend figure + a sparkline/line chart directly under it), and one or more list/table cards below or beside it.
- The iPhone mockup shows a single screen with: a top app header ("Copilot" wordmark, centered), a two-item tab/segment control directly under the header (one plain-text tab, one pill-highlighted/selected tab — this appears to be the screen-level navigation, e.g. switching between a summary view and a transactions view, but exact tab labels are illegible), then a stack of three vertically arranged card sections on one scroll: (1) a large balance/spend figure with a trend line, (2) a "review" list card, (3) a row of category/account "ring" indicators. Two more section headers are visible cut off at the very bottom edge, implying further content below the fold that is not captured in this image.

## Layout & content
- **iPhone screen, card 1 (top):** dominant white numeral **"$2,272 left"** (high-confidence read — largest, sharpest text in the whole composite) with a smaller line of grey subtext directly beneath it (illegible — too small even at 8× upscale). Below that sits a sparkline: a yellow/orange line rising left-to-right that terminates in a short green segment, capped by a green pill-shaped badge containing white text (word shape suggests something like a short status word, e.g. "ON TRACK" — not legible enough to quote). No axis labels or gridlines are visible. This pairs a money figure directly with a trend visualization and a colored status pill in the same card — status sits immediately adjacent to the figure it qualifies, consistent with law 8's "status adjacent to the number it qualifies," though it's a competitor pattern being observed, not a KEEL screen.
- **iPhone screen, card 2 ("To Review"-shaped label, small, upper-left of card; not fully certain):** a card containing two stacked rows. Each row has: a left-aligned merchant-like text (illegible), a right-aligned colored pill (row 1 orange/red pill with a small icon + word; row 2 green pill with a small icon + word), and a right-aligned dollar figure per row (both illegible at pixel level, e.g. "$7.xx"-shaped and another 3–4 digit figure). Under the two rows, centered, is a blue link-style text (looks like a single short call-to-action phrase, color and underline-less-but-blue-colored text consistent with a tappable link) — exact wording not legible.
- **iPhone screen, card 3 (rings row):** a row of 5 circular progress-ring badges, left to right: red ring, red ring, yellow/orange ring, yellow/orange ring, green ring. Each ring encloses a small flat-color icon glyph (colors: pink/red, pink/red, unclear light icon, blue/white icon, yellow icon) too small to identify subject matter. Under each ring is a dollar figure (all illegible) and a short caption (illegible). A section-level label sits upper-left of this row (illegible, short word) and what looks like a second label with a trailing ">" chevron sits upper-right (chevron shape is fairly clear and recurs elsewhere in the image as an apparent "see more/expand" affordance).
- **Below the rings row, cut off at the phone's bottom bezel:** two more section-header-looking short text strings side by side, no card content visible under either — the image crops before revealing what these sections contain.
- **Desktop window (upper-right mockup), main content:** a chart card labeled with a short heading (illegible) directly above a large white numeral **"$1,000 spent"** and a lighter grey subtext line beneath it (illegible). Under the numeral, a sparkline covering the full card width: starts yellow/orange (low, gently undulating upward), has one short red-colored segment partway along (a localized color change on the same continuous line — possibly marking a spike or an over-budget day), then a short green segment, continuing off the right edge of the visible crop. Below this chart card is a list card with a heading (illegible) and 3+ rows of text (merchant/description-like strings, all illegible at this resolution) with small colored badge glyphs at the right edge of the visible crop (green/orange, too small to read values).
- **Desktop window (lower mockup), main content:** shows the same "Monthly ___ / $1,000 spent" chart-card pattern on the left (identical figure and sparkline shape to the upper-right window — plausibly the same underlying dashboard screenshot re-used/re-cropped at a different angle for the composite, not two distinct app states). To its right, a second card headed by a short label (illegible, "Net ___"-shaped) showing two large white numerals side by side — **appears to read "100,000" and "5,000"** (moderate confidence only; digits are blurred) — each with a small colored delta/percentage-style text underneath (one green, one orange). Below these two numbers is a two-line comparison chart: a light-blue line and an orange line running roughly in parallel left-to-right, with small dot markers along the x-axis. Further right/below, a third card lists rows of text (illegible labels) each paired with a numeric value column and, at the far right, a short colored trend-line/sparkline segment per row (alternating orange and green) plus a rightmost column of very short labels that could be time-period abbreviations — none of this is legible enough to quote.
- **Density/alignment observed generally:** all dollar figures observed are right-aligned or centered under/beside their chart; every large money figure in this image has either a trend line, a colored badge/pill, or a delta percentage directly adjacent to it — no bare unlabeled number was found. Numbers use no visible currency symbol on the desktop card headline figures except "$1,000 spent" and "$2,272 left" (dollar sign present on both of those); the two large lower-desktop numerals ("100,000"/"5,000"-shaped) show no visible "$" prefix at this resolution, so currency marking on those two could not be confirmed.

## Controls inventory
- Search field (icon + text input) — top of sidebar, both desktop windows.
- Sidebar primary nav — ~6 vertically stacked items, first one shown in a selected/highlighted state (light-blue pill background). Exact labels not certifiable at this resolution (see Open questions); kind = nav list, apparent action = switch primary view.
- Sidebar secondary list — grouped rows with small colored dot bullets before each account/group name and a right-aligned figure on populated rows; kind = collapsible/grouped account list (chevron/disclosure affordance not clearly visible, so collapsibility is inferred from grouping pattern only, not confirmed).
- macOS window traffic-light controls (close/minimize/maximize) — top-left of both desktop windows; standard OS chrome, not app-specific.
- Toolbar icon buttons next to traffic lights (back/forward-looking chevrons + 1–2 more icon glyphs) — kind unclear (could be nav-history controls or view-mode toggles); icons too small to identify precisely.
- iPhone header row: a circular icon button (top-left, glyph illegible) and a rounded-square icon button (top-right, glyph illegible) flanking the "Copilot" wordmark.
- iPhone two-item tab/segment control directly under the header — one plain tab, one pill-highlighted (selected) tab; kind = segmented control, apparent action = switch between two sub-views of the home screen.
- Colored status/trend pill badges — seen in the iPhone "review" card (one orange/red pill + icon + word, one green pill + icon + word per row) and as the single green pill capping the top sparkline; kind = inline status badge, apparent meaning = directional/qualitative flag (color-coded, red/orange = attention or increase, green = on-track or decrease) inferred from color convention only, not from legible text.
- Blue link-style text under the iPhone "review" card — kind = tappable text link, apparent action = navigate to a fuller list (exact label illegible).
- 5 circular progress-ring badges on the iPhone (rings row) — kind = ring/progress indicator per category or account, each pairs a partial-arc fill (implying progress toward some threshold, e.g. budget-remaining) with a centered icon and a dollar figure + caption beneath; exact captions illegible.
- Chevron ">"-style glyph recurring near section headers (both iPhone rings-row header and desktop card headers) — kind = "view more/expand" affordance, apparent action = drill into the section; exact adjacent label text illegible on all instances.

## Flow steps
N/A — single static composite image, no sequential screens captured.

## States
- No empty, loading, or error states are observable — every visible surface shows populated data (numbers, chart lines, list rows present in all cards). No placeholder/empty-state copy is present anywhere in the image.
- The closest thing to a "state" signal is the color-coding on the trend lines and badges (red/orange vs. green segments on the same continuous sparkline, and red/orange vs. green pill badges in the review card), which implies the product distinguishes "attention-needed" vs. "on-track/good" states inline within a single chart/card rather than via a separate banner — but no copy confirms this reading; it is a color-only inference.

## Business rules implied
- A single spend/trend line can change color partway along its own path (yellow/orange → red → green segments observed on both the iPhone card-1 sparkline and the desktop "$1,000 spent" sparkline in `design/references/copilot/web-01.png`), implying the underlying chart encodes a per-point qualitative state (e.g., under/near/over some threshold) rather than a single static series color — a rule about the data model (each point on the series carries a status), not just a styling choice.
- The iPhone's card-1 large balance figure ("$2,272 left") is paired with a trend line ending in a status pill, implying the app computes and displays a "remaining" figure that is itself time-aware/trending, not a flat balance snapshot (image: iPhone mockup, `design/references/copilot/web-01.png`).
- The sidebar's account list groups individual accounts under type-level header rows with per-account colored dot bullets and right-aligned balances, implying accounts are organized by an account-type taxonomy for at-a-glance net-position scanning (image: both desktop mockups, `design/references/copilot/web-01.png`) — consistent with, but not proof of, a "grouped by type with subtotals" pattern; no subtotal figures were legible enough to confirm rollups are actually shown at the group-header level.

## Standout details
- Pairing a large "$X left/spent" numeral directly with an inline sparkline whose color changes along its own length (rather than a single flat-colored line) is a compact way to fuse "how much" with "how you're trending" in one glance — worth studying further at higher resolution before adopting, since the exact color semantics (what triggers red vs. orange vs. green on the same line) could not be confirmed here.
- Small colored dot bullets preceding each account name in the sidebar list give an at-a-glance color key per account without needing a legend, assuming the color is reused consistently elsewhere in the app (cannot confirm reuse from this image alone).
- The recurring ">" chevron next to multiple section headers (rings row, desktop card headers) suggests a consistent "every card/section has a drill-in affordance" convention across the whole app, which would be a good discoverability pattern if confirmed at higher fidelity.
- Circular progress-rings-with-centered-icon for per-category/account figures on the iPhone home screen is a distinct visual treatment from the plain sparkline card above it in the same screen — the app appears to deliberately vary chart form (line vs. ring) by content type on the same page rather than using one chart style everywhere.

## Open questions
- This unit's only file is a composite marketing/App-Store hero image, not a direct in-app screenshot; nearly all secondary text (sidebar nav labels, card headers, table rows, captions under rings, badge words, subtext lines) is below the legibility threshold even after 4–8× Lanczos upscaling and unsharp-mask sharpening in this session — attempts are documented above but none produced certifiable text. Any downstream synthesis should treat this unit as low-confidence/structural-only, not a source of exact copy.
- The desktop window chrome (macOS traffic lights) conflicts with this unit's "web" group label in the manifest — unclear whether this is actually a Mac-app screenshot mislabeled as "web," or a web app deliberately skinned to mimic native macOS chrome. Not resolved by this image.
- Whether the two desktop mockups (upper-right window and lower window) are two different screens/states of the app, or the same dashboard screenshot cropped twice at different angles for the composite — the identical "$1,000 spent" chart card in both suggests the latter, but this could not be fully verified since the surrounding cards differ (review/transactions list vs. net-worth panel).
- Exact wording of: the iPhone's two header tabs, the "To Review"-shaped section label and its list rows' merchant names/amounts, the rings row's per-ring captions, the sidebar's 6 nav-item labels, the account-group header labels, and both desktop windows' card headings and table contents — all illegible, not guessed.
- Whether the sidebar account-type groups (rows with dot bullets and no visible figure) are truly empty/collapsed groups or simply have figures too small to render in this composite — not settled.
- Whether the green pill badge capping the iPhone's top sparkline is a status label, a percentage, or something else — only its color and general pill shape are certain.
