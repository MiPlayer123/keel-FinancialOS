# Census — brex-community-02

## Evidence
- `design/references/brex/community-09-bill-pay-bills-table.png` → Brex web app, "Bills" (bill-pay) list screen, "For payment" tab active, showing the bill-pay queue table with account/nav chrome (top bar + left icon rail). Bottom of the table is cropped by the source image.
- `design/references/brex/community-10-ai-flagged-expense.png` → A single expense/receipt detail card annotated with an "AI flagged this expense" callout banner above it; styled as a marketing/promo graphic (decorative blue swirl background, not a plain product screenshot) rather than a literal in-app chrome capture.
- `design/references/brex/community-11-assistant-notifications.jpg` → A "Notifications" panel/dropdown listing dated notification groups, including one generated by "Brex Assistant."

## Information architecture
- Bills table (image 09) sits behind a top-level left-rail icon (the icon is highlighted/active with a shaded background, second-to-last visible icon before "people"/team icon at the bottom of the rail), confirming Bills is a first-class top-level nav destination, not nested under another section.
- Left icon rail (top to bottom, icons only, no text labels visible so meaning is inferred from position/glyph): profile photo circle → wallet/card icon → checkbox icon with a red unread dot → receipt/ticket icon → "V" lettered circle (workspace/entity switcher) → house icon → storefront/building icon → padlock icon → circular/pie icon → credit-card icon → document icon → document icon (active/shaded — the Bills page) → storefront icon → line-chart icon → people icon. No text labels are legible for any of these, so exact destination names cannot be confirmed beyond "Bills" (inferred from the active state coinciding with the Bills page).
- Top global bar (present on the Bills screen) holds, left to right: "Brex" wordmark/logo, then on the right: search (magnifying glass), notifications (bell), a book/guide icon, an "ai" icon (lowercase, pill-less, plain text glyph), "Support" button with a "?" icon, and a user-account menu "Olivia Wilson" with a dropdown chevron. This places global search, notifications, help/docs, an AI entry point, support, and account switching all in one persistent top bar, one click from anywhere.
- Within Bills, the four "quick action" cards (Pay a bill / Upload invoices / Edit policy / Bill pay settings) sit directly under the page title, above the status tabs — i.e., top-level actions are surfaced above the record list rather than tucked into an overflow menu.
- Bills are organized into four status tabs: "Drafts" (8), "For approval" (4), "For payment" (7 — active), "Processing / Complete" (no count badge shown). This is a single flat pipeline view; approval state and payment state share one tab bar rather than separate pages.
- The notifications panel (image 11) is grouped by day headers ("Today", "June 18, 2025"), newest first, each row carrying a description plus a "View" deep-link button — one click from a notification straight to (presumably) the record it references, though the destination screen itself is not shown in this evidence.
- The AI-flagged-expense card (image 10) is a receipt/expense record with an explanatory callout banner rendered above it; nothing in this image shows what list or workflow this card is embedded in (no surrounding nav chrome is visible), so its place in the IA cannot be confirmed from this evidence alone.

## Layout & content
- **Bills table (image 09):**
  - Page title "Bills", plain black bold text, no icon.
  - Quick-action row: 4 equal-width cards, each with a bold title, an orange circular arrow-icon button top-right of the card, and a lighter-gray one-to-two-line description below the title.
  - Tab row with numeric badges in light-gray pill/circle chips next to each tab label except "Processing / Complete", which has none visible.
  - Filter/toolbar row: "All bills (Default)" dropdown (eye icon + chevron), a search input (magnifying glass icon, empty, no placeholder text legible), a gear/settings icon button, and a "Download" button (download-tray icon + label) right-aligned.
  - Below that: "Add filter +" (plain, plus icon) and "Clear filters" (red text, red X icon) — the red color on "Clear filters" signals a destructive/reset action, distinct from the neutral "Add filter."
  - Table header row (gray background, gray label text): Due date | Vendor | Amount | Method | Status | Payment date | Invoice | Purchase order.
  - Table body, 4 rows fully or partially visible before crop:
    1. Apr 26, 2025 | Dropbox (blue circular brand icon) | **$1,280.00 USD** | Virtual card / •••• 1326 | Scheduled | Apr 20, 2025 | ✓ 56789GHI | PO-2024-024
    2. Apr 30, 2025 | Salesforce (brand icon) | **$21,000.00 USD** | ACH / •••• 0189 | Ready for payment | Apr 25, 2025 | ✓ IN-11134-123 | PO-2024-082
    3. Apr 30, 2025 | W hotels (brand icon) | **$15,108.71 CAD** with a smaller gray secondary line **$10,900.25 USD** beneath it | International wire / •••• 9187 | Ready for payment | Apr 25, 2025 | ✓ ABC12345 | PO-2024-115
    4. Apr 30, 2025 | Workday (brand icon) | **$5,500.00 USD** | ACH / •••• 3230 (partially cropped) | Ready for payment | Apr 25, 2025 | ✓ 123-PQR456 | PO-2024-091 (row cropped by image bottom edge)
  - Every money figure carries: (a) an explicit ISO-style currency suffix ("USD"/"CAD") rather than a symbol-only convention, (b) a payment "Method" with masked instrument (•••• + last 4 digits) directly adjacent, and (c) a "Status" cell immediately to its right-of-row context (Scheduled vs. Ready for payment) — status is column-adjacent to amount, satisfying "status adjacent to the number it qualifies."
  - The one foreign-currency row (W hotels, CAD) shows original currency as the primary (larger, dark) figure and the USD-converted equivalent as a secondary (smaller, gray) figure directly beneath — a clear primary/secondary visual hierarchy for currency conversion.
  - Invoice and Purchase order columns are right-side, lower-salience columns; Invoice numbers are prefixed with a green/dark checkmark (✓), implying a verified/attached-invoice state per row.
  - Density: table shows ~4 rows before crop, one row per line, no visible row grouping/subtotals in what's captured.
  - Alignment: Amount column is right-aligned; text columns (Vendor, Method, Status, dates, Invoice, PO) are left-aligned.

- **AI-flagged expense (image 10):**
  - Top callout banner (cream/pale-yellow background, orange text and orange flag icon): "This expense was flagged by Brex AI because:" followed by a single bullet "High risk item detected on receipt: Alcohol." The reason is rendered as a bulleted list, implying the format supports multiple simultaneous reasons even though only one is shown here.
  - Below, a stylized digital receipt card: dark-navy header band with a circular monogram logo ("C") and the merchant name "Carbonenewyork.com" in white.
  - Under the header: left-aligned gray label "Auto-generated by Brex"; right-aligned a green-outlined pill badge with a green checkmark reading "IRS compliant."
  - Large total figure "$213.75" in heavy black bold type — the single largest text element on the card, clearly the summary/hero number.
  - A dashed horizontal divider separates the total from itemization.
  - Line items (label left, amount right):
    - "Calamari Marco" — "$145.10" (plain weight)
    - "Chardonnay" — bold label, with a red/orange warning triangle icon immediately to the left of its amount — "$20.45" (the only line item both bolded and warning-flagged)
    - "Total, Surcharges, and Fees" — "$3.00"
  - Money-figure/status adjacency: the flagged dollar amount ($20.45, Chardonnay) has its warning icon placed directly beside the figure itself, not the label — literal adjacency of status-glyph to the specific number it qualifies.
  - Line items do not sum exactly to the hero total as displayed (145.10 + 20.45 + 3.00 = 168.55 vs. hero total 213.75), implying additional unlisted line items exist off-frame (the card likely scrolls or has more rows not captured in this image).

- **Notifications panel (image 11):**
  - Header "Notifications," bold, top-left, full-width divider beneath.
  - Grouped by date, each date group has a shaded gray full-width header row ("Today", "June 18, 2025").
  - Rows: a leading unread-status orange dot (present on the newest item only), notification body text (two lines wrap naturally), and a right-aligned outlined pill button "View" per row.
  - Row 1 (Today, unread — orange dot, dark/black text): "Brex Assistant added receipt and memo to $56.00 at Delta" — "View"
  - Row 2 (June 18, 2025, read — no dot, lighter gray text): "Your replacement card is out for delivery" — "View"
  - The one money figure present ($56.00) is embedded inline in a sentence, not in its own column; its "context" is the merchant name ("at Delta") and the fact this was an autonomous Brex Assistant action (receipt+memo attachment), not a status badge.
  - Read/unread state is encoded two ways simultaneously: presence/absence of the orange dot, and darker vs. lighter (grayed) body text color.

## Controls inventory
- **Bills table (image 09):**
  - Top bar: search icon button, bell/notifications icon button, book/guide icon button, "ai" icon button, "Support" button (with "?" icon), "Olivia Wilson" account-menu button (with chevron, opens dropdown).
  - Left rail: ~14 icon-only nav buttons (unlabeled in evidence); one shows a small red unread-indicator dot (the checkbox/task icon, third from top).
  - "Pay a bill" — card-style call-to-action button (orange circular arrow icon) → apparent action: start single-invoice bill creation/payment.
  - "Upload invoices" — card CTA (orange arrow) → apparent action: bulk invoice upload with AI-assisted draft-bill generation ("we'll scan them to generate a draft bill for each invoice").
  - "Edit policy" — card CTA (orange arrow) → apparent action: configure bill-pay approval/requirement policy.
  - "Bill pay settings" — card CTA (orange arrow) → apparent action: open accounts/permissions/ERP settings.
  - Tabs (filter by pipeline stage, mutually exclusive, each with a live count badge except the last): "Drafts" (8), "For approval" (4), "For payment" (7, active/underlined), "Processing / Complete" (no badge).
  - "All bills (Default)" — dropdown/saved-view selector with an eye icon and chevron; apparent action: switch between saved list views.
  - Search input (magnifying-glass icon, unlabeled) — free-text filter over the bill list.
  - Gear/settings icon button next to search — apparent action: table/column configuration (exact scope not confirmed).
  - "Download" button (tray-download icon) — apparent action: export the current table view.
  - "Add filter +" — apparent action: opens filter-builder UI to add a new filter condition.
  - "Clear filters" (red X icon, red text) — apparent action: reset all active filters; red styling signals a state-clearing/destructive-adjacent action distinct from ordinary buttons.
  - Table column headers (Due date, Vendor, Amount, Method, Status, Payment date, Invoice, Purchase order) — not confirmed sortable from this static image (no visible sort arrows/carets), so sort affordance is unconfirmed.
  - Per-row: masked payment instrument text ("•••• NNNN") functions as identity/context, not an interactive control in this evidence.

- **AI-flagged expense (image 10):**
  - No buttons are visible in this crop — the image shows only the flag-reason banner and the receipt/expense card content. No approve/reject/dismiss controls, no "why" expandable disclosure control, and no edit affordance are visible in the captured frame.
  - Badge: "IRS compliant" — a status pill (green check + label), appears informational/non-interactive in this crop.
  - Bolded line item "Chardonnay" with a red warning triangle icon functions as an inline flag on a specific line item, distinct from the top-level banner flag (implying the AI can flag both the whole expense and, at a more granular level, specific line items within it).

- **Notifications panel (image 11):**
  - "View" button — outlined pill button, one per notification row; apparent action: navigate to the referenced record (receipt/memo, or shipment tracking for the replacement-card notice). Appears identically styled regardless of read/unread state.
  - Orange dot — unread-status indicator, not a clickable control (no affordance shown, e.g., no visible "mark as read" interaction in this evidence).
  - Date-group header rows ("Today", "June 18, 2025") — visually appear to be static section dividers, not evidenced as collapsible/interactive in this image.

## Flow steps
N/A — the three images in this unit are independent, non-sequential screens (a list view, a standalone expense card, and a notifications panel) with no shared before/after state, click target, or confirmation trail evidenced between them. A plausible link (clicking "View" on the "Brex Assistant added receipt and memo to $56.00 at Delta" notification might lead to a card resembling image 10) is suggested by thematic similarity (both involve Brex's AI/Assistant acting on a receipt) but is not confirmed by any shared visual element, so it is not asserted as a flow.

## States
- Bills table: "For payment" tab is the selected/active state, visually marked by bold black label text plus a black underline beneath it (versus lighter-weight, non-underlined inactive tabs "Drafts", "For approval", "Processing / Complete").
- Bills table: no empty, loading, or error state is visible — the table is populated.
- Notifications panel: read vs. unread state is shown — unread row has an orange leading dot and darker body text ("Brex Assistant added receipt and memo to $56.00 at Delta"); read row has no dot and lighter/grayed body text ("Your replacement card is out for delivery"). No "no notifications" empty state, loading, or error state is visible in this crop.
- AI-flagged expense: shows a "flagged" state (orange/cream banner, orange flag icon, explicit reason text) layered on top of what is otherwise a normal receipt/expense record display (with its own success-style "IRS compliant" green-check badge coexisting alongside the risk flag) — i.e., a single record can simultaneously carry a positive compliance signal and a risk-flag warning. No error or loading state is visible.

## Business rules implied
- A bill's payment method is tied to a specific masked financial instrument (card or bank account, last 4 digits shown), and different bills in the same queue can use different methods (Virtual card, ACH, International wire) — image 09.
- Bills progress through a fixed, mutually exclusive pipeline of states before/around payment: Drafts → For approval → For payment → Processing/Complete — image 09 (tab structure with per-stage counts).
- Cross-border vendor bills carry a currency-conversion figure alongside the original currency, both rendered on the record (W hotels row) — image 09.
- Invoices attached to a bill are marked with a checkmark in the Invoice column, implying invoice-attachment/verification is tracked per bill as a discrete state — image 09.
- AI (Brex AI) can automatically flag an expense against a specific, named risk category ("High risk item detected on receipt: Alcohol"), and the reason is surfaced as human-readable, itemized text rather than a bare score or badge — image 10.
- An expense/receipt can be simultaneously "Auto-generated by Brex," marked "IRS compliant," and separately AI-risk-flagged — these are independent, non-exclusive states attached to the same record — image 10.
- The AI can flag at the line-item level, not just the whole-expense level: the "Chardonnay" line is individually bolded and marked with a warning icon distinct from the top banner — image 10.
- An autonomous agent ("Brex Assistant") can take a write action — attaching a receipt and a memo to a transaction — without it being framed in this evidence as an approval request; it appears in the notification feed as a completed, past-tense action ("added receipt and memo to $56.00 at Delta") — image 11.
- Notifications persist and remain browsable across multiple days with dated groupings, and carry read/unread visual state — image 11.

## Standout details
- The flagged-expense banner uses plain-language, itemized reasoning ("This expense was flagged by Brex AI because: • High risk item detected on receipt: Alcohol") rather than an opaque risk score — directly names the category (alcohol) and ties it to a specific line item, giving the reviewer an immediate, scannable "why."
- Co-locating a positive "IRS compliant" badge with a risk-flag banner on the same card is a deliberate contrast: the record isn't wholesale rejected, only the specific line item is called out — implies granular, additive flagging rather than record-level allow/deny.
- The masked-instrument convention ("•••• 1326") repeated per row keeps payment-method identity compact and consistent regardless of instrument type (card, ACH, wire) — one visual pattern for three different rails.
- Foreign-currency rows show original-currency-primary / converted-currency-secondary (smaller, gray, beneath) rather than replacing one with the other — preserves source-currency truth while still giving a usable comparison figure.
- "Clear filters" rendered in red (vs. every other neutral-styled control on the page) is a small but clear semantic-color usage: red reserved for a state-resetting action, consistent with "red = negative/attention" conventions.
- The notifications panel encodes read/unread doubly (dot presence + text color), which is more robust for accessibility/scanning than either signal alone.
- The "Brex Assistant added receipt and memo to $56.00 at Delta" notification phrases an AI action entirely in past tense as already-completed, with the dollar amount inlined directly into the sentence rather than pulled into a separate field — a conversational, low-friction way to surface an autonomous write.

## Open questions
- Whether the Bills page nav icon in the left rail has a text label anywhere (e.g., on hover) — no label is visible in the static capture, so its identity is inferred only from the active/shaded state coinciding with the Bills page.
- What the gear/settings icon button next to the Bills search box actually configures (column visibility? row density? something else) — not shown.
- Whether table column headers in image 09 are sortable — no sort indicators (arrows/carets) are visible, so this cannot be confirmed either way.
- What lies below the cropped bottom edge of the Bills table (rows beyond "Workday," and whether pagination/row-count controls exist) — not captured.
- Whether the "IRS compliant" badge and the AI risk-flag banner in image 10 are both persistent record state or whether the flag is a transient/dismissible reviewer-facing overlay — no dismiss/acknowledge control is visible in this crop, and no confirmation/undo affordance is shown for handling a flagged expense (e.g., how a reviewer clears or overrides the flag).
- Why the itemized line amounts in image 10 ($145.10 + $20.45 + $3.00 = $168.55) do not sum to the displayed hero total ($213.75) — implies additional line items exist off-screen/off-frame, but none are shown, so the gap cannot be attributed with certainty.
- Whether clicking "View" on the "Brex Assistant added receipt and memo" notification (image 11) leads to a card resembling the AI-flagged-expense view (image 10) — thematically plausible (both involve Brex AI/Assistant acting on a receipt) but not evidenced by any shared UI element across the two images.
- Whether unread notifications in image 11 support bulk actions (mark-all-read, dismiss) — no such controls are visible in the captured crop.
- The exact icon-to-destination mapping for the ~14 unlabeled left-rail icons in image 09 beyond the one confirmed active (Bills) — glyph shapes suggest generic categories (home, cards/wallet, tasks, storefront/merchants, security, reports, people/team) but none of these are asserted as fact since no labels are legible.
