# Census — quicken-simplifi-appstore-01

## Evidence
- `design/references/quicken-simplifi/appstore-quicken-simplifi-pad-01-2064x2752.jpg` → App Store marketing slide (iPad-sized), theme "cash flow forecasting": headline + a white card mockup showing a "Projected Cash Flow" line chart, a "Banking" net-cash summary, account-type breakdown rows, and two income-transaction pill rows.
- `design/references/quicken-simplifi/appstore-quicken-simplifi-pad-02-2064x2752.jpg` → App Store marketing slide, theme "Smart budgeting": white card mockup showing a "Left this month" hero number, a spending donut chart, and a category-breakdown list (Income/Bills/Planned Spend/Other Spend/Savings Goals) with colored icon swatches and chevrons.
- `design/references/quicken-simplifi/appstore-quicken-simplifi-pad-03-2064x2752.jpg` → App Store marketing slide, theme "Business & personal finances in one clear view": white card mockup showing a "Net Income" grouped bar chart (Income vs Expenses per month) with an overlaid Net Income trend line, three income-transaction pill rows, and the top of a second "Left this month" donut card.
- `design/references/quicken-simplifi/appstore-quicken-simplifi-pad-04-2064x2752.jpg` → App Store marketing slide, theme "retirement/investment planning": two stacked white cards — a "Retirement" stacked-area projection chart (age 55–75+) with a callout, and an "Investments" card with a portfolio total, a percent-change badge, and a value-over-time area chart.
- `design/references/quicken-simplifi/appstore-quicken-simplifi-pad-05-2064x2752.jpg` → App Store marketing slide, theme "Run your business confidently": three income-transaction pill rows, an "Invoices" section with a "Paid in full" success-status card, and a mock invoice detail (#1027) with From/To/Sent fields.

All five are iPad-format promotional/App-Store screenshots (rounded-corner phone/tablet-style white card mockups floating on a solid indigo/purple background with marketing headline + subheadline text above the card) — they are stylized composites for marketing, not necessarily 1:1 captures of a single continuous in-app screen. Treat exact adjacency of elements within a card as illustrative, not literal, per Open questions below.

## Information architecture
- No visible nav chrome (tab bar, sidebar, hamburger menu, or app header) appears in any of the 5 images — these are cropped/composited card mockups, not full-app screenshots, so overall navigation structure is NOT observable from this unit.
- Content is organized into discrete white rounded-corner "cards," each apparently representing one functional area: Cash Flow forecast, Budgeting ("Left this month"), Net Income (business), Retirement projection, Investments, and Invoices/business transactions.
- Within the budgeting card (pad-02), category rows (Income, Bills, Planned Spend, Other Spend, Savings Goals) are listed together under one "Left this month" total, each with a trailing chevron ">", implying each row is one tap away from a category drill-down/detail screen.
- Transaction-style pill rows ("Consultation +$150.00", "Cabinet Design +$375.00", "Installation +$900.00") recur verbatim across pad-01, pad-03, and pad-05, suggesting these are the same sample dataset reused across marketing slides for different feature callouts (cash flow, business net income, invoicing) rather than three distinct screens with independent data.
- The Invoices section (pad-05) groups a payment-confirmation status card directly above an invoice document layout (#1027), implying invoice list/detail and payment-status are co-located in the same business/invoicing area.

## Layout & content
- **pad-01 "Projected Cash Flow" card**: title "Projected Cash Flow" top-left; line chart with y-axis gridline labels $20k/$25k/$30k/$35k (no x-axis date labels except a "Today" vertical marker); a solid line segment before "Today" transitions to a dashed line after "Today"; one data point is called out with a floating label bubble "$31,452" positioned on the dashed (future/projected) segment. Below the chart, outside the chart card: "Banking" (small label) over "$87,762" (large bold hero number) with sub-label "Cash & Checking" — this is the only figure with a category label placed directly beneath it in bold hero style. Two more rows below, smaller text, right-aligned values, no bold: "Credit" → "-$10,553" and "Savings" → "$80,107". Then two separated pill-shaped rows (rounded full-height borders): "Consultation" → "+$150.00" and "Cabinet Design" → "+$375.00", both amounts in green.
- **pad-02 "Left this month" card**: "$1,415" large bold hero number, centered, with "Left this month" sub-label directly beneath in regular weight — status label sits below the number, not beside it. Below: a 5-segment donut/ring chart (colors: light blue largest, indigo/dark-blue second, green, yellow-green, magenta — segments un-labeled in the chart itself, color key comes from the list beneath). Below the donut, a list of 5 rows, each with a small colored rounded-square icon (white "+" for Income, white "−" for the 4 expense-type rows), a label, a right-aligned signed dollar amount, and a trailing chevron ">": "Income" +$4,000 (green amount, icon green), "Bills" -$1,385 (icon dark indigo), "Planned Spend" -$600 (icon light blue), "Other Spend" -$400 (icon magenta/pink), "Savings Goals" -$200 (icon yellow-green, row is cropped at the bottom edge of the image so only the icon and "-$200" plus chevron are partly visible). Negative values are rendered in plain black text with a leading "-", NOT in red.
- **pad-03 "Net Income" card**: title "Net Income" top-left; grouped bar chart, y-axis $2K/$4K/$6K/$8K, x-axis categories Apr/May/Jun/July; two bars per month (green = Income, indigo/purple = Expenses), plus a black dashed line with black dot markers tracking "Net income" per month, drawn above/independent of the bar heights. A legend row beneath the chart uses colored square swatches: green "Income", indigo "Expenses", black "Net income". Below the card, three pill rows (same visual style as pad-01/pad-05): "Consultation" +$8,350.00, "Cabinet design" +$375.00, "Installation" +$900.00 (all green). Below that, a second card begins: heading "Left this month" with the top arc of a donut chart visible before the image crops off.
- **pad-04 "Retirement" card**: title "Retirement"; a 3-band stacked area/streamgraph, y-axis $1m/$2m/$3m, x-axis ages 55/65/75; a callout "$2.4mil" in large bold text sits above a thin green vertical marker line intersecting the chart around age ~63; the three stacked bands are colored indigo (top/largest), green (middle), yellow-green (bottom) — no legend or labels identify what each band represents. Second card, "Investments" title; "$200,725" large bold hero number; directly beside/beneath it a green-outlined pill badge "↑ 0.92%" followed by plain green text "+$39.92 today" — three pieces of context (percent, dollar delta, "today" timeframe) all placed adjacent to the one figure. Below, an area chart (indigo fill, no visible x-axis labels) with y-axis gridlines $180k/$190k/$200k/$210k.
- **pad-05 slide**: three pill rows atop a light-gray rounded panel: "Consultation" +$150.00, "Cabinet Design" +$375.00, "Installation" +$900.00 (green amounts, identical data to pad-01/pad-03 subsets). Below, heading "Invoices". A light-gray card with a green circular checkmark icon, bold "Paid in full", regular-weight subtext "Your direct deposit payment of $1,110.00 was approved on Jun 1", and a second line "Confirmation #: 478567". Below that, an invoice-document mockup: bold "#1027" top-left; then three labeled rows each preceded by a thin horizontal divider line, label left-aligned / value right-aligned: "From" → "Verrow Designs / mo@verrowdesigns.com / (408) 272-0098" (3 lines, right-aligned, decreasing emphasis top to bottom), "To" → "Jack Pine / The Pineapple Company" (2 lines), "Sent" → "May 1".
- Number formatting is consistent across all 5 images: "$" prefix, comma thousands separators, hero/summary numbers shown with no decimals (e.g. "$87,762", "$1,415", "$200,725"), one abbreviated figure ("$2.4mil"), transaction/line-item amounts always shown with two decimal places and an explicit sign prefix ("+$150.00", "-$10,553" — note the account-balance figure "-$10,553" has no decimals, inconsistent with the two-decimal transaction pills).
- Density is low/summary-only in every card — no more than 5 list rows visible in any single card (budgeting card), consistent with marketing-slide framing rather than a working register; no evidence of a high-density transaction table in this unit.

## Controls inventory
- Chevron "›" at the right edge of each budgeting category row (pad-02: Income, Bills, Planned Spend, Other Spend, Savings Goals) — apparent action: drill into category detail/transactions.
- Colored rounded-square icon swatches with a white glyph ("+" or "−") prefixing each budgeting row (pad-02) — appear to encode category type (inflow vs outflow) via icon + color, not a tappable control per se, though could double as a category badge.
- Green-outlined pill/badge "↑ 0.92%" (pad-04, Investments card) — a compact status badge combining a directional arrow glyph, percentage, and border-only (non-filled) styling; distinct from the plain-text "+$39.92 today" beside it.
- Rounded pill-style rows for individual named transactions (pad-01, pad-03, pad-05: "Consultation", "Cabinet Design"/"Cabinet design", "Installation") — full-height rounded borders, label left, signed green amount right; no chevron on these, unlike the budgeting-category rows, suggesting these may not be drill-in rows (or the marketing crop simply omits it).
- Green circular checkmark icon (pad-05, "Paid in full" card) — success/status badge, not a control.
- No visible primary buttons (e.g. "Add transaction," "Create invoice," "Save"), text inputs, tabs, or filter/sort controls anywhere in this unit — consistent with these being read-only marketing captures rather than interactive-state screenshots.

## Flow steps
N/A — each image is an independent single-card marketing composite; no multi-step sequence is evidenced within this unit.

## States
- **Success/confirmation state** (pad-05): green circular checkmark + bold "Paid in full" + "Your direct deposit payment of $1,110.00 was approved on Jun 1" + "Confirmation #: 478567" — exact copy quoted. This is the only explicit state (beyond default/populated) observable in this unit; no empty, loading, or error states are shown in any of the 5 images.

## Business rules implied
- A cash-flow forecast distinguishes past/actual data from future/projected data by line style: solid before a "Today" marker, dashed after it (pad-01, "Projected Cash Flow" chart).
- Negative monetary values (account balance and expense-category totals) are rendered in plain black/dark text with a leading "-", not in red or any warning color — contradicts an assumption that red always marks negative money in this product (pad-01: "Credit -$10,553"; pad-02: "Bills -$1,385", "Planned Spend -$600", "Other Spend -$400", "Savings Goals -$200").
- Positive/inflow amounts are consistently rendered in green with a leading "+" across every card that shows signed amounts (pad-01, pad-02 "Income +$4,000", pad-03, pad-05).
- A monthly budget view nets "Left this month" from at least 5 category buckets (Income, Bills, Planned Spend, Other Spend, Savings Goals), each independently signed and totaled into one hero number (pad-02).
- Business net income is computed and charted per month as Income − Expenses, with a distinct "Net income" series plotted alongside the Income/Expenses bars rather than only shown as a single total (pad-03).
- Invoice payment confirmations record a payment method ("direct deposit"), an amount, an approval date, and a confirmation number as a discrete auditable unit (pad-05: "Your direct deposit payment of $1,110.00 was approved on Jun 1", "Confirmation #: 478567").
- Invoices carry a numeric identifier (e.g. "#1027") plus From/To parties (each with name, and for the "From"/business party, email and phone) and a "Sent" date (pad-05).

## Standout details
- Marketing headline styling gives the differentiating phrase a cyan/light-blue accent color while the rest of the headline stays white — consistent pattern across all 5 slides (e.g., "Know what's next" in cyan vs "with your money" in white on pad-01; "budgeting" in cyan on pad-02; "confidently" in cyan on pad-05) — a cheap, reusable way to visually tag the "feature name" within a longer headline.
- The retirement projection chart uses a 3-band stacked area (streamgraph-like) rather than a single line, hinting at a scenario range or a principal/growth/contribution breakdown — visually rich for a single-glance marketing image even though the bands are unlabeled.
- The Investments card stacks three distinct pieces of temporal context right next to the headline number: a bordered "↑ 0.92%" badge (relative/percent framing), plain "+$39.92 today" text (absolute framing), and implicitly the chart below (historical framing) — three complementary framings of the same delta, all adjacent to the number they qualify.
- The "$2.4mil" callout on the retirement chart uses an abbreviated unit ("mil") instead of full digits, saving space while calling out a specific future milestone value at a specific age.
- Reuse of an identical 3-line-item dataset (Consultation/Cabinet Design/Installation, an apparent small contracting/design business) across three different feature slides (cash flow, business net income, invoicing) — a single coherent persona/storyline threads the whole marketing set rather than generic placeholder data.

## Open questions
- These are App Store marketing composites, not raw app screenshots — exact spatial adjacency, cropping, and whether multiple cards shown together (e.g., pad-01's Banking summary directly above the Consultation/Cabinet Design pills) reflect one real continuous screen or a marketing collage of separate screens is not settled by this evidence.
- What the 3 colors in the retirement stacked-area chart (indigo/green/yellow-green) individually represent (e.g., contributions vs. growth vs. a second account; or optimistic/expected/conservative scenarios) is not labeled anywhere in the image and cannot be determined.
- What the 5 donut-chart colors in pad-02 map to beyond the order matching the list below (Income green, Bills indigo, Planned Spend light blue, Other Spend magenta, Savings Goals yellow-green) is inferred from color-swatch-to-row proximity, not from any legend directly on the chart — worth flagging as inferred, not directly labeled.
- Whether the "Consultation"/"Cabinet Design"/"Installation" pill rows are tappable (they lack the chevron seen on budgeting-category rows) or are purely decorative in these marketing crops is not settled.
- No navigation chrome, tab structure, or app-wide layout is visible in this unit, so this unit alone cannot establish where any of these six feature areas (cash flow, budgeting, business net income, retirement, investments, invoicing) sit in the product's information architecture — that requires evidence from the `quicken-simplifi-community-*` units.
- Image resolution/legibility was sufficient to read all quoted text in all 5 images; no illegible regions were encountered in this unit.
