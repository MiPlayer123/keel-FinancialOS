# Census — ynab-iphone-02

## Evidence
- `design/references/ynab/iphone-09-iPhone69_USD_5a.png` → App Store marketing screenshot (iPhone 6.9" size class). Foreground: a Siri/system-suggestion style card reading "There's $232.09 available in Groceries" with a Groceries category snippet inside it. Background: illustrated marketing chrome (gradient, cartoon dollar bills) with headline copy "Ask Siri to check your plan on the go" and, at the very bottom, the iOS home-screen system "Search" pill (confirms this is a lock/home-screen widget context, not an in-app screen).
- `design/references/ynab/iphone-10-iPhone69_USD_2.png` → App Store marketing screenshot. Foreground: a dark-mode in-app screen titled "Edit Plan" showing a "Cost to Be Me" total, a targets-vs-income progress-bar comparison, and a "Regular Expenses" list of goal-style budget line items. Headline copy "Find your 'Cost to Be Me'". Phone mockup is shown tilted/rotated as a design composition, truncating some row labels at the left edge.
- `design/references/ynab/iphone-11-Jobf4f9bcb6-14d2-4eb6-a151-43152096efb5-.png` → App Store marketing screenshot (promo-render filename, likely sourced from a video job export). Foreground: a light-mode "Home" screen showing a "Ready to assign" balance, a collapsible "Top Priorities" category list, and the start of a collapsible "October Summary" stat row. Headline copy "Get good with money." with "Get" circled in a hand-drawn-style green ink annotation.

## Information architecture
- None of the three images show a persistent tab bar, nav bar with siblings, or hamburger/menu affordance — each is a cropped/staged marketing frame, not a full chrome capture, so the surrounding navigation shell is not evidenced.
- Image 3 ("Home") reads as a landing/dashboard screen that bundles three distinct concerns in one scroll: (a) a single "Ready to assign" balance-and-action row, (b) a curated "Top Priorities" subset of budget categories (collapsible, with its own "Edit" control), (c) a collapsible "October Summary" stat block starting below the fold. This implies Home is a composite overview rather than a plain single-purpose list, with categories, unallocated cash, and monthly stats living one scroll away from each other rather than on separate top-level tabs.
- Image 2 ("Edit Plan") is reached via a circular back-chevron button top-left, implying it is a pushed/modal subview rather than a root tab. Its content is organized as: a plan-level total ("Cost to Be Me"), two comparison bars (spending targets vs. income), then a "Regular Expenses" section that itself contains multiple named sub-groups (row labels truncated, see Open questions), each sub-group carrying its own add (+) and overflow (...) controls plus an "Add Target" text link. This shows a two-level hierarchy: Plan → Expense Groups → individual Targets.
- Image 1 shows budget-category data (a single category's available balance) surfaced completely outside the app shell, through what reads as an iOS Siri/system-suggestion card triggered by voice ("Ask Siri to check your plan on the go"). This is architecturally one level removed from in-app navigation — a zero-navigation, system-level access point to a specific category balance.

## Layout & content
**Image 1 (Siri suggestion card):**
- Card region 1 (top, on translucent glass background): plain-text sentence "There's $232.09 available in Groceries" — the dollar figure is inline within prose, not isolated.
- Card region 2 (below, on cream/off-white rounded rect): a category row — shopping-cart emoji + "Groceries" (bold black, left-aligned) with a small tree/leaf logo icon at the far right of the same line; beneath it, the amount "$232.09" in bold black text inside a bright green rounded pill, left-aligned under the category name.
- Card region 3: full-width blue rounded "Done" button, bold white centered text.
- Below the card, unrelated iOS chrome: green illustrated dollar-bill graphics and a system "Search" pill at the very bottom — confirms lock-screen/widget context, not an app screen.
- Number formatting: "$232.09" — two decimal places, dollar sign, comma-free (value under $1,000).

**Image 2 (Edit Plan, dark mode):**
- Header region: circular translucent back-chevron (top-left), centered white text "Edit Plan", to its right the system status icons (signal, wifi, battery — all near-full).
- Hero region directly under header: very large bold white "$4,000.00", with "Cost to Be Me" in smaller white text directly beneath it (label sits under the number here, not beside it — different pattern from Image 1/3).
- Progress-bar region (one rounded dark panel containing two stacked bars):
  - Row 1: "July's Targets" (white label, left) with a bright green horizontal gradient bar shown fully filled, and "$4,000.00" in green text right-aligned at bar's end.
  - Row 2: "Monthly Income" (white label, left) with a solid gray bar, uniformly gray with no visible fill differentiation, a small pencil icon just left of the value, and "$6,000.00" in white text right-aligned at bar's end.
  - The two bars are the same width/track length, inviting direct visual comparison of committed target total against income.
- List region ("Regular Expenses" section header, bold white): rows are partially obscured by the tilted-phone marketing composition, cutting off the left portion of each label. Visible pattern per row: a bold, larger dollar figure (e.g. "$133.16", "$33.33", "$555.55") stacked above a smaller, gray subtext line giving a target amount and a date (e.g. "$799.00 by Dec 20", "$100.00 by Sep 30", "$5,000.00 by Mar 3"). This is a two-tier money display: computed/near-term figure on top, underlying goal + deadline below it in reduced-emphasis gray.
- Density: at least 3 distinct expense-target rows are visible (partially), grouped under at least 2 sub-headers (also truncated, see Open questions), each sub-header paired with its own "+"/"..." icon row and an "Add Target" link. This is a detail-level (not summary-level) view — every row shows its own figure, not a rolled-up total (aside from the top "Cost to Be Me" hero figure).
- Alignment: money figures are consistently right-aligned when paired with a left-aligned label on the same row (progress-bar rows); money figures are left-aligned/stacked when they are the primary subject of a card row (hero figure, expense-row bold figures).

**Image 3 (Home, light mode):**
- Header region: bold black "Home" title (large, left-aligned) with a circular "..." overflow button top-right, mint/teal-tinted translucent background.
- "Ready to assign" region: single white rounded full-width card — green pill "$50.00" (left) + plain black text "Ready to assign" (same line, immediately right of the pill) + "Assign" button (light-blue rounded pill, right-aligned, same row).
- "Top Priorities" region: section header row with a downward chevron (collapse/expand) + bold black "Top Priorities" text (left), and a light-blue "Edit" pill button (right). Below it, three white rounded card rows, one per category: emoji icon + category name (left, black bold), green rounded pill with dollar amount (right) — "🛒 Groceries — $373.79", "🍽 Eating Out — $432.90", "🪴 Home Improvement — $200.00". All three follow identical layout/alignment.
- "October Summary" region (partially visible, cut off at bottom of frame): section header row with downward chevron + bold black "October Summary" text. Below it, two side-by-side white square-ish tiles begin to appear: left tile has a target/bullseye icon (top-left) and an "Edit >" link (top-right) with "Total Targets" label and "$5,075.36" figure below (plain black text, no pill); right tile has a small pie-chart icon (top-left) with "Underfunded" label and "$280.89" figure below (also plain black text, no pill).
- Number formatting: all figures use "$" + comma-free thousands under $10,000 except "$5,075.36" which does use a comma — consistent standard USD two-decimal formatting throughout.
- Density: 3 category rows fully visible before the "October Summary" section begins; the summary section's two tiles are cut off by the image/frame bottom, so any further content below is not evidenced.

## Controls inventory
- **Image 1**: "Done" — button, full-width blue rounded rect, bold white label, dismisses/confirms the Siri suggestion card. (iOS system "Search" pill visible below is OS chrome, not app-owned.)
- **Image 2**: circular back-chevron (top-left) — navigates back from Edit Plan. Pencil icon beside "Monthly Income" $6,000.00 — apparent inline-edit affordance for the income figure. Two circular "+" buttons (add — one per expense group visible) paired with two circular "..." buttons (overflow/more options — one per expense group visible), positioned together at each group's header row. Two "Add Target" blue text links, one per visible group, appearing beneath a group's rows — apparent action to add a new goal/target line item to that group.
- **Image 3**: circular "..." overflow button (top-right of Home header). "Assign" — light-blue pill button beside the "$50.00 Ready to assign" row — apparent action to allocate the unassigned balance. Downward chevron disclosure control beside "Top Priorities" (collapsible section). "Edit" — light-blue pill button beside "Top Priorities" header — apparent action to edit which categories appear in this curated list. Downward chevron disclosure control beside "October Summary" (collapsible section). "Edit >" link with target-icon, inside the "Total Targets" tile — apparent shortcut into a targets-editing view.
- Badges with meaning: bright-green rounded pill consistently marks a currently-available/spendable dollar amount (used for the Siri category balance, all three Top-Priorities category balances, and the Ready-to-assign figure). Plain black text (no pill) is used instead for computed/aggregate stats that are not directly spendable — "Total Targets $5,075.36" and "Underfunded $280.89" in the October Summary tiles, and the hero "$4,000.00 Cost to Be Me" figure in Edit Plan. This is a consistent visual distinction between "money you can act on" and "money as a reported total."

## States
- No empty, loading, or error states are captured in any of the three images — all three show populated, ostensibly normal ("success") states.
- Image 1's Siri card can be read as an ambient/glanceable acknowledgment state — a passive check-in surface with a single dismiss ("Done") action, rather than a data-entry or error state.
- No zero-dollar or negative-dollar figures are shown in any image; the lowest visible figure is "$33.33" and all pilled figures are positive.

## Business rules implied
- A single budget category's live "available" balance can be queried and displayed via Siri/system integration outside the app shell (image `iphone-09`, headline "Ask Siri to check your plan on the go" + card "There's $232.09 available in Groceries") — implies per-category balances are exposed as a queryable, voice-accessible data point, not locked behind in-app navigation.
- Budget "targets" distinguish a computed periodic contribution amount from an underlying goal amount and deadline: rows in image `iphone-10` show a bold figure ("$133.16", "$33.33", "$555.55") paired with a smaller "target amount by date" line ("$799.00 by Dec 20", "$100.00 by Sep 30", "$5,000.00 by Mar 3") — implies the system derives a required-per-period contribution from a stored (goal amount, target date) pair, and surfaces the derived number with more visual weight than the source inputs.
- A plan-level total ("Cost to Be Me," $4,000.00) is presented directly against "Monthly Income" ($6,000.00) as two aligned progress bars of matching track length (image `iphone-10`) — implies the product models total committed monthly spend and total monthly income as directly comparable, bar-chart-able quantities within the same screen, without requiring a separate report.
- Unallocated cash ("Ready to assign," $50.00, image `iphone-11`) is surfaced with an adjacent "Assign" action on the same row — implies a zero-based-budgeting rule where leftover money is tracked as a distinct, actionable balance rather than silently absorbed into an account total.
- A separate "Underfunded" aggregate ($280.89) is tracked alongside "Total Targets" ($5,075.36) in the same October Summary section (image `iphone-11`) — implies each target/category carries a funding-status computation that rolls up into a portfolio-level shortfall figure, distinct from the sum of target amounts themselves.
- Category rows use exactly one emoji as their icon across all observed instances (🛒 Groceries, 🍽 Eating Out, 🪴 Home Improvement) — implies categories are modeled with a single icon slot, not a composite icon/badge system.

## Standout details
- Uniform green-pill treatment for any "money currently available to spend" figure, contrasted with plain black/white text for computed totals (Total Targets, Underfunded, Cost to Be Me hero) — an at-a-glance visual grammar for "actionable balance" vs. "reported number" that requires no reading of labels to distinguish.
- Siri/system-level surface (image `iphone-09`) delivering a single category's balance as a tappable glanceable card with zero in-app navigation — a "check in without opening the app" pattern.
- Paired progress bars (Targets vs. Income) sharing one track length in "Edit Plan" turns a plan-vs-income comparison into a purely visual read, without a table or separate chart.
- Two-tier money display on goal-style rows — a bold "what to fund this period" number stacked directly above a dimmed "what it's for, and by when" line — separates the actionable figure from its justification/proof without needing a details screen.
- "Cost to Be Me" is used as a quoted, named product concept in marketing copy ("Find your 'Cost to Be Me'"), suggesting it functions as a branded term for total monthly committed spend rather than a generic label.
- Consistent per-group control pairing in Edit Plan: every visible expense group carries the same "+" (add) and "..." (overflow) icon pair at its header, plus its own "Add Target" text link beneath its rows — a repeatable, predictable affordance pattern applied uniformly across groups.
- Hand-drawn-style circle annotation around the word "Get" in the "Get good with money." tagline (image `iphone-11`) — a marketing-only detail, not app UI, but notable as a soft/approachable tone-setting device.

## Open questions
- No persistent tab bar or nav chrome is visible in any image, so how "Home," "Edit Plan," and the Siri integration relate structurally (e.g., whether Home is a tab root, whether Edit Plan is reached only from Home) is not settled by this evidence.
- The exact scope of "Top Priorities" — a manually pinned subset, an algorithmically surfaced subset, or literally all budget categories when few exist — is not shown; only that it is collapsible and has its own "Edit" control.
- What the "..." overflow controls (Home header, and the two per-group overflow buttons in Edit Plan) actually open is not shown — no submenu/sheet is captured.
- What tapping "Assign" does (opens a modal, an inline numeric entry, an auto-distribution) is not shown.
- Row labels in Edit Plan are truncated by the tilted-phone marketing composition ("...ebrations", "...one", "...ance") — likely partial words (e.g., possibly "Celebrations," a name ending "-one," and a word ending "-ance" such as "Insurance") but cannot be confirmed from the visible pixels.
- Whether the gray "Monthly Income" bar represents 0% fill or some non-zero-but-obscured fill amount is not clearly resolvable — the bar reads as uniformly gray with no visible internal contrast.
- Time/status-bar chrome differs across all three images (9:41, 9:41, 12:29) and battery level in image `iphone-11` appears low/partial (~30–40%) versus full in the other two — confirms these are independently staged marketing captures, not a continuous session, so no cross-image state continuity can be inferred.
- Whether the app surfaces an explicit "surplus" or "deficit" label when comparing Cost to Be Me against Monthly Income (beyond the raw bar comparison shown) is not evidenced in this crop.
