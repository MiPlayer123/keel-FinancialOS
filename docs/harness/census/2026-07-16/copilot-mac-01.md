# Census — copilot-mac-01

## Evidence
- `design/references/copilot/mac-01-AppStoreArt_macOS_4.png` → Category drilldown detail panel for "Transportation" (App Store marketing crop; left nav sidebar not visible), overlaid with headline "Budgets that make sense for you."
- `design/references/copilot/mac-02-AppStoreArt_macOS_2.png` → Full app window (with macOS traffic-light chrome and full left nav sidebar) on the "Cash flow" screen, overlaid with headline "Cash flow tracking at your fingertips."
- `design/references/copilot/mac-03-AppStoreArt_macOS_5.png` → "Recurrings" screen, two-pane: list of recurring items (left) and detail of one recurring merchant, "Spotify" (right); marketing crop, no sidebar; headline "Never lose track of subscriptions."
- `design/references/copilot/mac-04-AppStoreArt_macOS_7.png` → "Accounts" screen, two-pane: aggregate Assets/Debt summary (left) and single account detail, "Chase Credit Card" (right); marketing crop, no sidebar; headline "All your accounts. Finally in one place."
- `design/references/copilot/mac-05-AppStoreArt_macOS_3.png` → Transaction detail pane for a single transaction ("Sunoco"), with sliver of the transaction list visible at far left edge; marketing crop; headline "Spending categorization, powered by AI."
- `design/references/copilot/mac-06-AppStoreArt_macOS_6.png` → "Investments" screen, two-pane: aggregate Performance/Benchmark summary (left) and single brokerage account detail, "Robinhood" (right); marketing crop, no sidebar; headline "All your investments. Benchmarked."
- `design/references/copilot/mac-07-AppStoreArt_macOS_1.png` → Full app window (with macOS traffic-light chrome and full left nav sidebar) on the "Dashboard" screen showing Monthly spending, Assets/Debt, Transactions to review, and Top categories cards; headline "Private. Safe. Smart."

All seven images are Copilot Money macOS App Store marketing screenshots. Two (mac-02, mac-07) show the full window chrome including the left navigation sidebar; the other five are cropped to a content pane only (sidebar not visible), so IA claims below draw primarily on mac-02 and mac-07.

## Information architecture
- Left nav sidebar (visible in mac-02, mac-07), top to bottom:
  1. A utility row above the nav list: circular refresh/reload icon, then back (`←`) and forward (`→`) chevron arrows (macOS-style history navigation), positioned in the sidebar's top strip next to traffic lights.
  2. A search field, placeholder text "Search", with a magnifying-glass icon, full width of sidebar.
  3. Primary nav list, one icon + label per row, in this order: **Dashboard**, **Transactions** (with a numeric badge — "3" in mac-02, "6" in mac-07), **Cash flow**, **Accounts**, **Investments**, **Categories**, **Recurrings**.
  4. The currently active screen is shown as a highlighted (filled blue) row in the nav list — "Cash flow" is highlighted in mac-02 while its content pane shows Cash flow; "Dashboard" is highlighted in mac-07 while its content pane shows the dashboard. Nav highlight and displayed screen are consistent.
  5. Below the primary nav, a section header "MY ACCOUNTS" (small caps, muted).
  6. Under "MY ACCOUNTS", accounts are grouped by a collapsible group header naming an account type/subtype, each with a dropdown chevron (▾): "Credit cards" (mac-02, mac-07), "Depository" (mac-07), "Investment" (mac-07, cut off at bottom of crop before its rows are visible).
  7. Each group lists individual accounts as rows: a colored dot bullet, account nickname (sometimes truncated with "…", e.g. "Plaid Diamond 12.5%...", "Plaid Silver Standard...", "Plaid Gold Standard 0..."), and a right-aligned balance amount.
- Transactions "to review" and "Top categories" are surfaced as their own dashboard cards (mac-07), one click away from Dashboard via "VIEW ALL ↗" links, implying a dedicated Transactions review queue and a Categories drilldown are each a separate destination reachable from the dashboard summary.
- Each aggregate content screen (Accounts, Investments, Cash flow evidenced) follows a two-pane pattern: a left "roll-up" pane showing an aggregate metric + chart + time-range tabs, and a right pane that is a per-item detail (single account / single holding-group) with its own chart and time-range tabs. Recurrings follows a similar two-pane list+detail pattern (list of recurring items on the left, one recurring item's detail/rules on the right).
- A transaction can be inspected in a detail pane that sits beside/replaces the transaction list (mac-05: sliver of list at left edge, detail pane occupies the rest), suggesting master-detail layout for Transactions similar to Recurrings.
- Category drilldown (mac-01) shows a "Category" header pane containing: a summary of one category (icon, name, month-to-date spend, remaining budget), a bar chart of historical monthly spend for that category, a "Key metrics" expandable section with yearly aggregates, and a filtered transaction list for the current month below — i.e., budget summary, historical trend, KPI table, and underlying transactions are all one click deep inside a single category, not spread across separate screens.

## Layout & content
### Sidebar accounts (mac-02, mac-07)
- Row format: colored dot (color appears to encode account type/status, not explained in evidence) · account name (truncatable) · right-aligned dollar balance, no cents shown at this density (e.g., "$7,335", "$4,623", "$410", "$200", "$100").
- Numbers are plain, no coloring by sign in the sidebar list (mac-07 shows credit-card balances as plain white/light text, not red, even though they are presumably liabilities).

### Dashboard (mac-07)
- Three-plus card grid below the title "Dashboard":
  - **Monthly spending** card: header label "Monthly spending" with "TRANSACTIONS ↗" link top-right; body shows "$73 over" in bold red as the headline figure, with "$1,696 budgeted" directly beneath it in smaller muted text — the adjacent context for the money figure is the budget ceiling it's being compared against. A line/area chart (red) tracks cumulative spend with a dashed projection segment continuing to a callout bubble "$619 over" (red) at the projected month-end point. Two distinct "over" numbers coexist in the same card: current overage ("$73 over") and projected month-end overage ("$619 over") — status (over/under) is expressed by color (red) and by the word "over" directly attached to the number, not by a separate badge.
  - **Assets/Debt** card: "Assets" labeled with a blue dot, figure "$24,253", green pill badge "↗ 999%"; "Debt" labeled with a red dot, figure "$121,974", red pill badge "↗ 999%"; "ACCOUNTS ↗" link top-right. A dual-line chart (flat, then a sharp upward spike near the right edge for both series) sits below, with time-range tabs "1W 1M 3M YTD 1Y ALL" (pill-style; "1M" is the selected/active pill, shown filled/highlighted).
  - **Transactions to review** card: header "Transactions to review", "VIEW ALL ↗" link top-right; a "TODAY" sub-header (all-caps, muted) groups rows below it. Each row: an unchecked checkbox, merchant name ("United Airlines", "Touchstone Climbing" — the latter has its own small icon), a category-guess pill badge ("OTHER" muted gray; "GYM" dark green with a climbing-person icon), a dollar amount (green "$500.00" for United Airlines; muted "$78.50" for Touchstone Climbing), and a trailing small dot "•" after the amount on both rows.
  - **Top categories** card: header "Top categories", "VIEW ALL ↗" link top-right. Rows: colored dot, category name ("Restaurants", "Shops", "Gym"), a horizontal bar (bar length appears to encode relative spend), and dollar amount(s). Restaurants shows "$1,106" appearing on both sides of its bar (identical value repeated); Shops shows "$500" on both sides (identical value repeated); Gym shows "$157" then, after its bar, "$79" — a different, smaller number, unlike the other two rows. This asymmetry is unexplained by the evidence (see Open questions).
- Density: Dashboard shows 4 cards simultaneously, each card summary-only (aggregate figure + chart + at most 2 transaction/category rows) — no raw ledger detail on this screen.

### Cash flow (mac-02)
- Header row: title "Cash flow"; filter control "Year to date" (pill/dropdown with chevron) showing resolved range "Jan 1, 2024 - Aug 20, 2024"; "compared to" label; comparison dropdown "No comparison" (chevron); far right, a labeled toggle "Show excluded spend" (off/unfilled state).
- Card "Net income": "VIEW MORE ↗" link top-right; sub-label date range "Jan 1 - Aug 20, 2024"; headline figure "$15,743.12" in green (positive net income shown in green, consistent with "red = negative money only" convention).
- Bar chart: y-axis single labeled tick "$3K" (no full axis); monthly green bars Jan 2024 through Dec 2024 — bars exist for months after the current date (Sep–Dec), i.e., forward months are pre-rendered/placeholder rather than omitted. The current month (Aug 2024) carries a small pill label "Now" pinned above its bar, distinguishing "current" from past/future in the same series without a color change.

### Recurrings (mac-03)
- Left pane header "Recurrings" with a "+" add button (square icon button).
- Summary card: "$25 left to pay" (left-aligned figure + muted sub-label "left to pay"), a circular progress ring in the center (mostly filled, small unfilled gap — visually encoding "paid so far" proportion), "$2,754 paid so far" (right-aligned figure + muted sub-label). The two dollar figures flank the ring and are each captioned directly beneath by their status ("left to pay" / "paid so far").
- "This month" collapsible section header (chevron), then a flat list of upcoming/settled recurring charges: each row = due date ("Aug 1st", "Aug 4th", "Aug 5th", "Aug 5th"), merchant/payee icon, merchant name (one truncated: "Property Payment Ren…"), cadence label ("Monthly" for all four visible), a small category/type icon in a colored circle, a dollar amount (right-aligned, largest is "$1,984.00"), and a checkmark "✓" at the far right of every row shown — implying all four listed items are already marked paid/confirmed for the period.
- Right pane, detail of one recurring series ("Spotify"):
  - Header "Monthly recurring" with "…" overflow button.
  - A pill badge "SUBSCRIPTIONS" (magenta/pink) sits above the merchant name.
  - Merchant row: headphone icon + "Spotify" (bold).
  - Right-aligned: "NEXT PAYMENT" (muted label) above "$9.99" (bold) above "around Sep 1st" (muted, approximate date language — "around", not an exact date).
  - "RULES" label (muted, all-caps) followed by rule chips describing the recurrence-detection logic in natural language fragments: "Named Spotify", "from $6 to $15", "on any day of the month", "every month" — each chip mixes plain and bold text (e.g., "Named **Spotify**", "from **$6** to **$15**"), reading as an auto-generated sentence broken into chip fragments.
  - A horizontal timeline/sparkline: dots connected by a line from "OCT '23" through "SEP" (month tick labels along the bottom), with the last segment rendered as a dashed line ending in an open (hollow) circle — visually distinguishing confirmed past occurrences (solid dots, solid line) from a projected future occurrence (dashed line, open dot).

### Accounts (mac-04)
- Left card "Assets"/"Debt" roll-up: "Assets" (blue dot) "$32,177" with green pill "↗ 2.23%" beneath; "Debt" (orange/red dot) "$12,854" with a green pill "↘ 1.77%" beneath — notably the Debt change arrow points down (↘, i.e., debt decreased) and is still colored green, i.e., color here encodes favorability (debt going down is good, shown green) rather than literal sign. A gear/settings icon sits top-right of this card. Below: a dual-line chart (Assets in blue, Debt in orange/red), both largely flat with a single step change, and time-range tabs "1W 1M 3M YTD 1Y ALL" with "1M" the active pill.
- Right card, single account detail: institution glyph (blue circle icon), account last-4 "4258", "Updated 5 hours ago" (muted italic, staleness indicator), and a top-right pill badge "● 50.16%" (amber/gold) — positioned where a utilization percentage would sit for a credit account. Account name "Chase Credit Card" (bold) with balance pair "$7,524.20 / $15,000.00" (current balance / credit limit, slash-separated, right-aligned). Chart: an orange line trending upward then dropping sharply to a green flat line at the right edge (reading as balance paid down to near zero), same "1W 1M 3M YTD 1Y ALL" tabs with "1M" active.
- "Manage connection" button (gear icon + text) and "…" overflow button sit at the top of the right/detail pane.

### Investments (mac-06)
- Same two-pane skeleton as Accounts. Left card "Performance" (teal dot) "↗ 0.35%" captioned "Your accounts"; "Benchmark" (purple dot) "↗ 0.02%" captioned "AAPL" (the benchmark is a specific ticker, not a market index in this evidence) — gear icon top-right. Dual-line chart (teal vs. purple lines crossing several times) with the same "1W 1M 3M YTD 1Y ALL" tabs, "1M" active.
- Right card: institution glyph (a green icon resembling a feather/quill), account last-4 "3779", "Updated 10 hours ago". Top-right: two green pills, "+ $20.90" and "↗ 0.41%" (absolute change and percent change shown as two separate adjacent badges, both green for a gain). Account name "Robinhood" (bold), balance "$5,010.00" (right-aligned). A green upward-trending line chart, same time tabs, "1M" active.
- "Manage connection" button and "…" overflow button at top of the detail pane, identical placement/wording to the Accounts screen — this chrome (Manage connection / time tabs / update-freshness caption) appears to be a shared component reused across Accounts and Investments detail cards.

### Category drilldown (mac-01)
- Header: "Category" (label, not the actual category name) with "Edit Budget" button (pencil icon + text) and a "…" overflow button.
- Category identity row: small colored dot, an emoji icon (🚌) in a circle, category name "Transportation" (large bold), right-aligned block: "Spent in Aug" (muted label) above "$6.33" (bold) above "$5.40 left" (smaller, muted, blue-tinted) — three-line right-aligned block giving label → actual spend → remaining budget, directly stacked so the remaining-budget status is immediately under the dollar figure it qualifies.
- Bar chart: a long horizontal series of monthly bars spanning roughly 3 years (axis labels compressed to single letters: M J J A S O N D J F M A M J J A S O N D J F M A M J J A S O N D), bars mostly orange, with two bars rendered red (appears to flag months where the category went over some threshold) and two bars rendered green (appears to flag under-threshold/low-spend months). A horizontal reference/average line runs across the chart with a callout bubble "$11.73" anchored to it (average monthly spend indicator, matching the "Average monthly spend" figure for 2023/2024 in the table below).
- "Key metrics" collapsible section: chevron toggle + a "?" help icon (tooltip affordance) beside the section title. A small table with year column headers "2022 2023 2024" and two labeled rows: "Total spent per year" ($52.32 / $140.76 / $88.44) and "Average monthly spend" ($4.36 / $11.73 / $11.73).
- "August 2024" sub-header, followed by a transaction row for that category/month: unchecked checkbox, date "Aug 5", merchant "Uber Pool", a category pill badge "🚌 TRANSPORTATION" (teal, all-caps), amount "$6.33" (right-aligned) — matching exactly the "Spent in Aug" total above it, implying (in this cropped view) only one transaction posted to Transportation in August.

### Transaction detail (mac-05)
- Left edge (list context, mostly cropped out): a toolbar with search icon, "+" add button, and a download/export icon; below it, partial rows of the transaction list are visible showing only amounts — "$36.66" (row highlighted/selected, dark active background), "$45.11" with a small blue dot beside it (unreviewed/unconfirmed marker, consistent with the "3"/"6" Transactions badge count seen in the sidebar), and "$40.30" (cut off at the bottom edge of the crop).
- Detail pane header: "Regular transaction" (label naming the transaction's type/kind), "Split" button (icon), "Recurring" button (icon), "…" overflow button.
- Calendar icon + "Tuesday, Aug 20, 2024" (full weekday-inclusive date format).
- Merchant "Sunoco" (large bold), right-aligned amount "$36.66" (large bold) — the amount for a single, unsplit transaction is shown at large/hero size directly beside the merchant name, the same numeral duplicated from the list row.
- "Category" and "Account" column labels side by side, each with a value beneath:
  - Category value: an icon-only circular badge (blue circle containing a snowflake/asterisk-like glyph, no visible text label) immediately followed by a pill badge "🚗 CAR" (green, all-caps). Two adjacent badges under one "Category" label is unexplained by the evidence — see Open questions.
  - Account value: a pill badge "🅾 Total Checking 8640" (blue, contains an institution glyph, the account nickname, and the last-4 digits together in one control).
- "Notes" label visible at the very bottom edge of the crop, field contents not visible/cut off.

## Controls inventory
- Sidebar: refresh icon, back arrow, forward arrow, search input, 7 nav items (Dashboard, Transactions[badge], Cash flow, Accounts, Investments, Categories, Recurrings), collapsible account-type group headers (chevron toggle), per-account row (click target, implied).
- Dashboard: "TRANSACTIONS ↗", "ACCOUNTS ↗", "VIEW ALL ↗" (×2) — all appear to be the same link pattern (label + outward arrow glyph) reused for "go to full screen" navigation from a summary card. Row-level checkboxes on "Transactions to review" items (unchecked in both visible rows).
- Cash flow: date-range dropdown pill ("Year to date"), comparison dropdown pill ("No comparison"), toggle switch ("Show excluded spend", shown off), "VIEW MORE ↗" link.
- Recurrings: "+" add button, "…" overflow (right pane), collapsible "This month" section, per-row checkmark "✓" (status indicator, not clearly an interactive control vs. a settled-state badge — ambiguous from evidence), rule chips (appear descriptive/read-only rather than directly editable in this view, though "RULES" chip format resembles filter/condition builders seen in Ramp/Simplifi evidence from other units).
- Accounts / Investments: "Manage connection" button (gear + text), "…" overflow, time-range tab group "1W / 1M / 3M / YTD / 1Y / ALL" (pill-style single-select, repeated identically on both the roll-up chart and the per-account chart), settings gear icon on the roll-up card.
- Category drilldown: "Edit Budget" button (pencil + text), "…" overflow, "Key metrics" chevron-collapsible section with a "?" help/info icon, per-transaction checkbox.
- Transaction detail: "Split" button, "Recurring" button, "…" overflow, category pill, account pill, icon-only badge next to category pill (function unclear).
- Badges/pills with apparent meaning: category pills are colored + all-caps + carry an emoji icon (🚌 TRANSPORTATION teal, 🚗 CAR green, GYM dark green with climbing icon, OTHER gray/muted); percent-change pills are green/red matched to favorability not raw sign (Debt ↘ 1.77% still green); a gold/amber "●50.16%" pill on a credit card reads as a utilization indicator; a magenta "SUBSCRIPTIONS" pill labels a recurring-charge type distinct from category pills.

## Flow steps
N/A — each image is a single static screen; no multi-step sequence is evidenced across this unit's files (they are independent marketing crops of different screens, not a captured task flow).

## States
- "Now" pill marker on the Cash flow bar chart marks the current month within a chart that otherwise renders past and future months identically (mac-02).
- Checkmark "✓" shown on every visible Recurrings list row — no un-checked/pending example is present in this evidence to contrast against (mac-03).
- Blue dot beside a transaction amount in the list sliver ("$45.11 ●") appears to be an unreviewed-item marker, contrasted with the two other visible rows ("$36.66" selected/active, "$40.30") that show no dot — but no explicit label/tooltip text is visible to confirm this reading (mac-05).
- No empty, loading, or error states are visible in any of the seven images — all show populated data.
- Approximate/uncertain-language copy: "around Sep 1st" (Recurrings next-payment date, mac-03) — the only instance of hedged/approximate copy observed in this unit, versus exact dates elsewhere ("Tuesday, Aug 20, 2024").

## Business rules implied
- A category has a monthly budget with a computed "left" remainder shown adjacent to month-to-date spend (mac-01: "$6.33" spent / "$5.40 left" for a category, implying a budget of roughly $11.73 for Transportation in August — which also matches the chart's plotted average-line callout "$11.73").
- Monthly spend is tracked and compared against a per-category historical average, with over/under-average months flagged distinctly (red/green bars) in the same chart as the plain (orange) bars (mac-01).
- Overall monthly spending is tracked against a single budgeted total with both a live overage and a separately projected month-end overage surfaced together ("$73 over" current vs. "$619 over" projected) (mac-07).
- Recurring charges can have a detected amount *range* rather than one fixed value ("from $6 to $15" rule chip for Spotify), implying the system tolerates/expects price variance on a subscription and still classifies it as the same recurring series (mac-03).
- Recurring detection encodes day-of-month flexibility explicitly ("on any day of the month"), implying recurrence matching does not require a fixed calendar day (mac-03).
- Credit card accounts display a running balance against a credit limit as a pair ("$7,524.20 / $15,000.00") plus a derived utilization percentage badge ("50.16%") (mac-04).
- Investment "Benchmark" comparison is configurable to a specific ticker (shown as "AAPL"), not fixed to a market index, implied by the "Benchmark" card explicitly labeling its comparison target by ticker (mac-06).
- Transactions can carry two independent flags — a review checkbox and a possible "unreviewed" dot marker — suggesting review state is tracked at the transaction level distinct from any category-confidence signal (mac-05, mac-07).
- A single transaction detail exposes both "Split" and "Recurring" as first-class one-click actions alongside the core Category/Account/Notes fields, implying both operations are common enough to warrant top-level buttons rather than being nested in an overflow menu (mac-05).

## Standout details
- The "left to pay" / progress-ring / "paid so far" summary card (mac-03) communicates a whole-month recurring-bills progress at a glance with only two numbers and one ring — no table needed to see "how much of this month's bills are done."
- Rule chips rendered as a natural-language sentence broken into clickable-looking fragments ("Named **Spotify**", "from **$6** to **$15**", "on any day of the month", "every month") — a legible way to expose an underlying matching rule without a form.
- The recurring timeline sparkline uses solid-dot/solid-line for history and dashed-line/open-dot for the next (unconfirmed) projected occurrence — a lightweight, label-free way to distinguish fact from forecast on the same axis (mac-03).
- Debt's favorable direction (declining) is colored green even though the arrow glyph itself points down (↘) — color is bound to "is this good for the user," not to arithmetic sign or arrow direction (mac-04). This is a concrete, evidenced counterexample to a naive "red=down, green=up" rule and worth deliberately deciding for KEEL given Law 8 ("red = negative money only").
- "Around Sep 1st" — hedged, human date language for a predicted (not yet posted) recurring charge, contrasting with exact dates used for posted transactions ("Tuesday, Aug 20, 2024") (mac-03 vs. mac-05).
- The "?" help icon placed directly beside the "Key metrics" section header (mac-01) suggests inline definitional help for a metrics table, rather than a separate help/glossary page.
- Reused chart chrome (identical "1W 1M 3M YTD 1Y ALL" tab set, identical "Manage connection"/"…" button placement, identical "Updated N hours/ago" freshness caption) appears verbatim across Accounts and Investments detail cards, evidencing a shared "linked account summary" component.

## Open questions
- The unlabeled circular icon-only badge (blue circle, snowflake/asterisk glyph) sitting immediately before the "CAR" category pill on the transaction detail (mac-05) is not identified by any visible text — its function (a second category? a status/confidence marker? an AI-suggestion indicator?) cannot be determined from this evidence alone.
- Whether the blue dot beside "$45.11" in the transaction-list sliver (mac-05) means "unreviewed," "pending," or something else is not confirmed by any legible label in this crop.
- The "Top categories" card's Restaurants and Shops rows show the identical dollar figure on both sides of their bar ("$1,106"/"$1,106", "$500"/"$500"), while the Gym row shows two different figures ("$157" then "$79") in the same visual position — the evidence does not explain whether this is two different metrics (e.g., budget vs. actual) rendered inconsistently, a data artifact specific to this screenshot, or an intentional per-row variant (mac-07).
- Whether the Recurrings list checkmarks ("✓") are a static "already paid" status or a toggleable checkbox-equivalent control cannot be determined — no unchecked example or interaction affordance is visible (mac-03).
- Whether "999%" shown for both Assets and Debt YoY-style change badges on the Dashboard (mac-07) is a real computed value, a capped/clamped display value, or a placeholder/demo-data artifact is not resolvable from the image alone — it is identical for both series, which is suspicious but not explained.
- Colored dot bullets used throughout (sidebar account rows, Assets/Debt legends, Top categories rows) are clearly a legend/series-identity device, but the evidence never shows a legend key mapping specific colors to specific meanings beyond the two-series Assets/Debt cases — for the sidebar's individual account dots and the three-row Top categories dots, the color-to-meaning mapping is not stated anywhere in view.
- No image in this unit shows an empty state, a loading state, an error/disconnected-account state, or a multi-transaction split-in-progress state — this unit's evidence is exclusively "happy path, fully populated" screens.
