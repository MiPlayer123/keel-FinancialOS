# Census record — `<unit-id>` (`<source-app>` / `<flow>`)

<!--
One record per manifest unit. Structured observation, not prose summary —
record what is OBSERVABLE in the evidence. Do not editorialize about whether
KEEL should copy it; that judgment happens in synthesis. Do not invent
anything not visible in the evidence (invention is the #1 census failure
mode). "Observation outranks documentation."
-->

- **Unit:** E-NNN · **Evidence:** `<files>` · **Observed:** `<app + date>`

## Information architecture
Where this screen/flow lives (nav path), what it is grouped with, what is one
click away vs. buried.

## Layout & content
Regions and what each contains. For each money figure: what status/context
sits adjacent to it. Density (rows visible, summary-vs-detail split).

## Controls inventory
Every interactive control: label, kind (button/select/inline-edit/…), what it
appears to do. Note bulk actions, keyboard affordances, filters, sort.

## Flow steps (sequences only)
Numbered do → see steps across the screenshots. Note confirmations,
optimistic updates, undo affordances.

## States observed
Empty / loading / error / success states visible in the evidence. Exact copy
of error and confirmation text where legible (this encodes business rules).

## Business rules implied
Only rules DIRECTLY evidenced (e.g. "pause requires an until-date", "delete is
two-step with typed confirmation"). Cite which screenshot shows each.

## Mobile / responsive (if captured)
What collapses, what disappears, what moves at narrow width.

## Open questions
Things the evidence doesn't settle (for synthesis to resolve or defer).
